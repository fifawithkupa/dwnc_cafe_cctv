# 판정을 계속 좋게 만들려면 — 무엇을 남기고 무엇을 배울까

2026-09-11 대화 정리. 매장이 늘어난 뒤에도 판정이 계속 좋아지게 하려면
무엇을 기록하고 그 기록으로 무엇을 할 수 있는지를 적는다.
`CLAUDE.md` 의 전제(매장당 카메라 1대, 매장별 튜닝 금지, 3상태 정직하게)를 깔고 쓴다.

---

## 0. 핵심 긴장

우리가 파는 약속은 **"아무것도 안 남는다"** 인데,
모델이 좋아지려면 **뭔가는 남아야 한다.** 이 둘은 같이 가질 수 없다.

그래서 설계의 첫 단추는 기술이 아니라 **무엇까지 남길지 정하는 것**이다.
정하고 나면 그 결정을 안내문과 규제 샌드박스 신청서에 **정직하게 적는다.**
감추고 가면 나중에 훨씬 비싸진다.

---

## 1. 남길 수 있는 것은 세 단계다

| 단계 | 남는 것 | 배울 수 있는 것 | 잃는 것 |
|---|---|---|---|
| ① 숫자만 | 지금 앱에 나가는 것과 같음 | 없음. 틀렸다는 사실조차 모른다 | 없음 |
| ② 위치 기록 | 사람 네모 좌표, 관절 각도, 연결된 의자, 짐과 걸친 비율 | **대부분** | 설명이 한 줄 늘어난다 |
| ③ 사진 | 화면 그대로 | 사람·물건을 찾는 능력 자체 | "영상 미저장" 약속 |

**권고: ②를 주 경로로 삼고, ③은 설치 당일 한 번으로 한정한다.**

②는 이미 벌어지고 있다. 엣지 박스가 판단할 때마다 한 줄씩 쓰고 기본 14일 보관한다
(`--keep-days 14`). 지금은 디버그 로그지만, **의도적인 학습 자료로 다시 설계해야 한다** —
형식을 고정하고, 버전을 붙이고, 보관 기간을 근거 있게 정한다.

---

## 2. 두 층으로 나눠서 봐야 한다

시스템은 두 층이다. **이 둘을 섞으면 아무것도 못 고친다.**

- **아래층 "찾기"** — 화면에서 사람·의자·가방을 찾아 네모를 그린다.
  좋게 만들려면 **사진이 필요하다.** 로그로는 못 고친다.
- **위층 "판정"** — 찾아낸 네모들을 보고 "이 자리는 찼다"를 결정한다.
  **사진이 필요 없다.** 숫자만 있으면 된다.

그리고 **지금 겪는 실패의 대부분은 위층 문제다.** 사유 코드가 그걸 말해준다:
하반신이 가려 각도를 못 잼, 관절점 부족, 옆 사람이 누구 자리인지 모호,
손님 증거 없음. 전부 "못 찾아서"가 아니라 **"찾긴 했는데 해석을 못 해서"** 다.

확인 결과 위층을 다시 학습시킬 재료는 **이미 로그에 다 있다.**
자리마다 그 순간 판정, 흔들림을 걸러낸 판정, 앱에 실제로 나간 값,
연결된 의자, 짐과 그 짐이 이 자리에 걸친 비율까지 남는다.

---

## 3. 정답은 어디서 오나 (싼 것부터)

1. **시간 일관성 — 공짜.**
   어떤 자리가 10분 내내 "사용중"인데 중간 한 번만 "모름"이면, 그 한 번은 틀린 것이다.
   안정된 답을 정답으로 삼아 그 순간을 다시 채점한다. 사람이 라벨을 안 붙여도 정답지가 쌓인다.
   **한계:** 일관되게 틀리는 문제는 못 잡는다. 항상 틀리면 그게 정답이 되어버린다.

2. **문 닫은 시간 — 공짜, 그리고 강력하다.**
   영업이 끝나고 아무도 없을 때 화면에 보이는 물건은 **전부 가구·장식이다.**
   라벨링도 애매함도 없는 완벽한 정답이다. 4절에서 이걸 쓴다.

3. **손님 앱 피드백 — 매장 부담 0.**
   앱에서 가끔 "자리 정보가 맞았나요?" 를 묻는다. 부담이 **매장이 아니라 손님에게** 간다는 게 핵심.
   `CLAUDE.md` 의 "매장에 수작업 요구 금지" 를 안 깬다.
   신호는 거칠지만 **일관되게 틀리는 걸 잡아주는 유일한 바깥 눈**이다.

4. **설치 당일 한 번의 촬영 — 매장당 정답지.**
   매장마다 정답지가 없으면 A매장을 좋게 만든 변경이 B매장을 망가뜨려도 모른다.
   안내문 붙이고 동의받은 상태에서 30분쯤 찍고, 우리가 앉았다 일어났다 하며 라벨을 단다.
   한 번으로 끝나고 범위가 정해져 있어 설명하기 쉽다. `CLAUDE.md` 의 "설치 시 1회 검수" 와 맞는다.

5. **상시 사진 수집 — 하지 않는다.** 얻는 것보다 잃는 게 크다.

---

## 4. 실패 유형별로 고칠 수 있나 — 네 경우

현장에서 나올 실패를 넷으로 보고, 각각 **로그 분석 + 코드 수정으로 풀리는지** 판정한다.
**절반만 풀린다.** 이 구분이 이 문서에서 제일 중요하다.

| 경우 | 로그로 고치나 | 이유 |
|---|---|---|
| 장식물을 손님 짐으로 오인 | **된다 · 가장 쉽다** | 장식은 움직이지 않는다 |
| 짐을 찾긴 했는데 규칙에 걸려 버림 | **된다** | 버린 이유가 로그에 남는다 |
| 사람·짐을 아예 못 찾음 | **로그로는 안 된다** | 탐지기가 네모를 안 내놓으면 아래층에서 할 게 없다. 다른 길 5개는 4-3 |
| 지갑을 인식 | **이름으로는 안 된다** | 탐지기에 "지갑" 항목이 없다. 4-3(4) 로 우회는 된다 |

### 4-1. 장식물 오인 — 된다, 그리고 가장 쉽다

**지금 규칙:** 사람이 아닌 물건 중 **제외 목록 36개에 없으면 전부 손님 짐으로 친다**
(`EXCLUDED_OBJECT_CLASSES`, `engine/seatnow_core.py:54`).
제외 목록에는 화분·TV·시계·침대 같은 게 들어 있다.

**빈틈:** 꽃병(`vase`)·병(`bottle`)·장식용 책(`book`)은 **제외 목록에 없다.**
테이블에 꽃병 하나 올려둔 매장이면 **그 자리는 영업시간 내내 "사용중"** 이 된다.

**고치는 법 두 가지.** 둘 다 사진 없이 로그만으로 된다.

- **(a) 제외 목록에 추가.** 어떤 자리에서 어떤 이름이 계속 뜨는지 로그에 다 남는다.
  꽃병이 3번 테이블에서 14일 내내 떴으면 장식이다. **단순하지만 매장마다 다른 물건이 나온다.**
- **(b) 움직이지 않는 물건을 걸러낸다 — 이쪽이 본질이다.**
  손님 가방은 왔다가 간다. 장식은 절대 안 움직인다.
  **문 닫은 시간에 보이는 물건 = 전부 장식**이라는 공짜 정답을 쓴다.
  영업 전/후 화면으로 매장별 "장식 지도" 를 자동으로 만들고, 그 자리 그 물건은 짐으로 세지 않는다.

  > **주의:** 이건 매장별 상태를 운영 중에 학습하는 것이라 `CLAUDE.md` 의
  > "주기적 재캘리브레이션 전제 금지" 와 스칠 수 있다. 사람 손이 안 가므로 금지 대상은 아니지만,
  > **장식을 옮기거나 손님이 가방을 몇 시간 두면 잘못 배울 수 있다.**
  > 그래서 **영업시간과 문 닫은 시간에 "둘 다" 있는 물건만** 장식으로 친다.
  > 손님 가방은 새벽 3시에 없다. 이 조건 하나로 대부분 걸러진다.

### 4-2. 짐을 찾긴 했는데 버려짐 — 된다

탐지기가 가방 네모를 냈는데도 위층 규칙이 버리는 경우가 있다.
자리를 절반 넘게 채우는 물건은 **가구를 잘못 본 것으로 보고 버린다**
(`MAXIMUM_OBJECT_SEAT_AREA_FRACTION = 0.50`). 빈 테이블 상판이 닫힌 노트북과
모양이 같아서 생긴 규칙이다. 반대쪽 하한도 있다.

**이 0.50 은 한 각도 6틱, 물건 34개에서 나온 숫자다.** 표본이 작다.
매장이 늘면 같은 방식으로 다시 확인해야 하고, **버려진 사례가 로그에 다 남으므로**
비율만 다시 그어보면 된다. 사진 필요 없다.

### 4-3. 사람·짐을 아예 못 찾음 — 로그로는 안 된다, 다른 길이 다섯 개 있다

탐지기가 네모 자체를 안 내놓으면 **아래층 문제다. 로그 분석으로는 못 고친다.**
대신 아래 다섯 가지가 있다. **싼 것부터** 적는다.

**(1) 카메라를 다시 단다 — 코드 문제가 아니다.**
자리가 화면 가장자리에 걸렸거나, 기둥에 가렸거나, 너무 멀면 **어떤 코드도 못 살린다.**
카메라는 우리가 고르고 우리가 단다 (`CLAUDE.md`). 설치 당일 한 시간이면 끝나고 효과가 제일 크다.
**로그로 구분은 된다:** 특정 자리만 늘 사람 0명이고 화면 가장자리면 설치 결함이다.

**(2) 다섯 장 중 한 장만 보여도 인정한다 — 제일 싼 코드 수정.**
지금은 한 틱마다 **5장을 보고 자리마다 다수결**을 한다
(`--median-frames 2`, `aggregate_burst_observations`).
**가방이 5장 중 2장에만 보이면 투표에서 져서 사라진다.**
짐에 한해서 "한 번이라도 보이면 인정" 으로 바꾸면 놓치는 게 크게 준다.
- 부작용: 장식 오탐이 같이 는다. → **4-1(b) 장식 지도가 정확히 이걸 막는다. 짝으로 간다.**
- 비용 0. 속도 변화 없음. **정답지로 오늘 바로 시험 가능하다.**

**(3) 작아서 못 본 거면 크게 다시 본다 — 2단계 보기.**
멀리 있는 가방은 화면을 줄여서 모델에 넣는 순간 뭉개진다.
**자리 위치는 이미 안다**(설치 때 그린 배치도). 그러니 전체를 싸게 한 번 본 뒤,
**"모름" 으로 나온 자리만** 그 부분을 원래 크기로 잘라 다시 본다.
- 다투는 자리에만 비용을 낸다. 대부분의 틱은 추가 비용 0.
- **주의:** 7.5초 예산. 그리고 **크기를 박아둔 OpenVINO 모델은 `--imgsz` 를 무시한다**
  (`CLAUDE.md`). 자른 그림을 넣어도 자기 크기로 답하면 아무 효과가 없다.
  모델을 그 크기로 다시 내보내야 한다. **속도만 보고 합격 선언 금지.**

**(4) 이름을 몰라도 "평소와 다른 것" 을 본다 — 지갑까지 푸는 유일한 길.**
탐지기가 지갑을 모르는 건 못 고친다. 하지만 **"이 테이블이 빈 상태와 다르다" 는 건 이름 없이 알 수 있다.**
4-1(b) 와 같은 재료를 쓴다: **문 닫은 시간의 화면이 곧 "아무것도 없는 상태" 의 기준**이다.
그 기준과 지금을 비교해 자리 안에 없던 게 생겼으면 **손님 증거로 친다.**
- 지갑·컵·종이·책 등 **이름 없는 물건을 전부 커버한다.**
- **약점:** 빛이 크게 흔들린다. 창가 햇빛, 조명 변화, 의자를 옮긴 것도 "달라졌다" 로 잡힌다.
  그래서 **판정이 아니라 증거 하나로만** 쓴다. 시간대별 기준을 따로 두면 많이 완화된다.

**(5) 더 큰 모델 · 재학습 — 마지막.**
`yolov8n` 을 한 단계 키우면 더 잘 찾지만 느려진다. **7.5초 예산부터 확인.**
재학습은 사진이 필요하다 (3절 4번, 설치 당일 촬영). **지갑은 여기서만 항목으로 추가된다.**

**권하는 순서: (2) → (4) → (3) → (5).** (1) 은 이 전부의 전제다.

> **(2)(3)(5) 는 전부 속도나 모델을 건드린다.**
> `CLAUDE.md` 규칙이 그대로 적용된다: **72칸 정답지로 다시 채점**(`accepted=70 wrong=2`),
> **한 번에 하나씩**, 그리고 **"빠른데 틀린" 변화를 조심한다.**

### 4-4. 지갑 — 영영 안 된다

탐지기는 COCO 80개 항목으로 학습됐고 **그 안에 "지갑" 이 없다.**
지갑은 아무것도 아닌 것으로 보이거나, 운 좋으면 휴대폰·책으로 잘못 잡힌다.
**로그 분석으로 절대 안 풀린다.** 지갑을 잡으려면 사진을 모아 항목을 추가해 재학습해야 한다.

가방·노트북·휴대폰은 COCO 에 있어서 잡힌다. **지갑만 예외다.**
현실적으로는 **지갑을 포기하고** 가방·노트북으로 커버되는 걸 노리는 게 맞다.
지갑만 놓고 자리 비우는 손님은 드물다.

---

## 5. 예시 — 손으로 그은 선을 데이터가 다시 긋는다

**지금 규칙 (`engine/seatnow_core.py:500`):**
테이블에 가려 무릎·발목이 안 보이면 각도를 못 잰다. 이때 딱 하나만 본다.

```
사람 네모의 세로 ÷ 가로 ≤ 1.75  →  앉은 것으로 본다
세로 ÷ 가로 >  1.75             →  모르겠다고 답한다
```

뭉툭하면 테이블 뒤 손님, 길쭉하면 서 있는 직원이라는 직관이다. 나쁘지 않다.
**문제는 1.75 가 눈대중이고, 네모 모양 하나만 본다는 것이다.**
같은 순간 의자가 붙어 있었는지, 가방이 올려져 있었는지를 전혀 안 쓴다.

**로그에 같이 남는 것:** 세로÷가로 · 연결된 의자 수 · 짐이 걸친 비율 ·
사람 확신도 · 주변 사람 수 · **그리고 잠시 뒤의 결론.**

마지막이 핵심이다. 그 순간엔 몰라도 몇 초 뒤엔 답이 분명해진다.

**할 수 있는 것 셋:**

1. **선 위치 검증.** 전부 비율 순으로 늘어놓고 결론을 붙인다.
   실제로 2.1 근처에서 갈린다면 **우리는 앉은 손님을 한참 버리고 있는 것이다.**
2. **선 하나로 부족함이 드러난다.** 비율이 애매해 지금은 버려지는 것들 중
   의자가 붙었고 짐이 절반 걸친 사례만 모아본다. 거의 다 찬 자리였다면:

   ```
   비율 애매 + 의자 붙음 + 짐 걸침  →  찬 자리
   비율 애매 + 의자도 짐도 없음     →  모름 유지
   ```

3. **공짜 검증.** 새 규칙을 쌓인 기록에 다시 돌린다. 카메라도 매장도 필요 없고
   몇 초면 지난 2주치를 다 채점한다. 모름은 줄었는데 틀린 게 늘면 버린다.

**이건 모델 재학습이 아니다.** 찾는 능력은 그대로 두고 해석 규칙만 바꾼다.
그래서 사진이 필요 없고 엣지 박스 성능도 그대로다.

---

## 6. 반드시 피할 것

### 6-1. 확률을 반올림하지 않는다

데이터로 규칙을 뽑으면 "찬 자리일 확률 51%" 같은 답이 나온다.
**51% 니까 찬 자리로 답하고 싶어진다. 하면 안 된다.**
모름을 없앤 게 아니라 **모름을 찍기로 바꾼 것**이다.
손님이 갔더니 자리가 없는 상황이 정확히 이렇게 생긴다.

확실히 높을 때만 찬 자리, 확실히 낮을 때만 빈자리, **가운데는 모름으로 남긴다.**
그리고 개선했다고 말하기 전에 **두 숫자를 따로 본다: 모름이 줄었나, 틀린 게 늘지 않았나.**
앞만 좋아지고 뒤가 나빠졌으면 개선이 아니다.

### 6-2. 매장별로 기준을 다르게 맞추지 않는다

매장이 늘면 "저 매장만 안 맞으니 저기만 숫자를 조금 바꾸자" 는 생각이 **반드시** 든다.
그러면 매장 수만큼 다른 시스템이 생기고, 어떤 변경이 전체를 좋게 했는지 **영원히 모른다.**
매장마다 다른 것은 **자리 배치 하나로 충분하다.** 그건 이미 설치 때 사람이 그려서 확인한다.

예외로 볼 여지가 있는 건 4-1(b) 의 장식 지도뿐이다. 그건 임계값이 아니라
**그 방에 원래 뭐가 있는지**를 적은 것이라 자리 배치와 같은 범주다.

---

## 7. 순서대로 할 일

1. **로그를 학습 자료로 다시 설계한다.** 형식 고정, 버전, 보관 기간 근거.
   지금은 코드가 바뀌면 형식도 바뀌는 디버그 로그다.
2. **시간 일관성으로 공짜 정답을 캔다.** 흔들린 판정을 안정된 답으로 다시 채점.
3. **문 닫은 시간으로 장식 지도를 만든다.** 4-1(b). 오탐이 제일 크게 줄 곳.
4. **`--log-detections` 를 켜고 버린 탐지를 본다.** 4-3 의 "절반" 을 확인.
5. **손님 앱 피드백을 붙인다.** 일관된 오답을 잡는 유일한 바깥 눈.
6. **매장마다 설치 당일 정답지를 만든다.** 매장이 2개가 되는 순간부터 필수.
7. **그 다음에야** 사진을 모아 탐지기 재학습을 생각한다. 지갑은 여기서만 풀린다.

## 8. 열린 문제

- ~~위치 기록을 박스 밖으로 내보낼 것인가~~ → **보내기로 결정. 9절에 설계.**
- **규제 샌드박스 신청서에 뭐라고 썼는지 확인이 안 됐다.**
  "영상 및 파생 데이터를 저장하지 않는다" 고 썼다면 2절 ②부터 다시 짜야 한다.
  → 안내문 문구(`문큐연락/안내문시안/`)도 여기에 걸려 있다.

---

## 9. 전송 설계 — 로그를 서버로 보낸다 (2026-09-11 결정)

### 지금 상태: 학습 재료를 매일 버리고 있다

박스가 Supabase 로 보내는 것은 **두 가지뿐**이다 (`edge/publish.py`).

| 올라가는 것 | 내용 | 시점 |
|---|---|---|
| `cafe_live` | 자리별 사용중 여부, 판정 상태, 모름 사유 | 판단할 때마다 |
| `cafe_seat_sheets` + 사진 1장 | 앱 팀이 이름표를 붙일 좌석 지도 | 설치 때 1회 |

**판정 기록(JSONL)은 안 올라간다.** 박스 안에만 쌓이고 14일 뒤 지워진다
(`--keep-days 14`). 즉 2절 ②의 재료를 **매일 버리고 있다.** 보내야 한다.

### 하지 말 것 셋

1. **로그를 통째로 보내지 않는다.**
   지금 JSONL 은 **코드가 바뀌면 형식도 바뀌는 디버그 로그**다.
   통째로 파이프를 꽂으면 **나중에 누가 필드 하나 추가했을 때 아무도 검토 안 한 정보가 조용히 매장 밖으로 나간다.**
   사고는 이렇게 난다.
2. **손님 앱이 쓰는 표에 섞지 않는다.**
   → **전용 표를 따로 만들기로 결정했다 (2026-09-11).** 같은 프로젝트 안이어도 표가 분리되면
   앱 동작에 영향이 없다. 다만 **RLS 로 앱에서 못 읽게 막는다** (10-4).
   남는 걱정은 섞임이 아니라 **양**이다. 그래서 10-3 의 "고르는 규칙" 이 필수다.
3. **실시간으로 보내지 않는다.** ⬅️ **가장 흔한 착각**
   앱이 보는 자리 상태는 실시간이어야 한다. **학습 자료는 아니다.**
   밤에 한 번 묶어서 보내면 충분하고, 더 싸고, 나가는 정보도 적다.

### 할 것 넷

1. **나가는 항목을 명시적으로 정한다.**
   허용 목록을 한 곳에 적고, **목록에 없는 필드가 생기면 시험이 깨지게** 한다.
   "무엇이 매장을 떠나는가" 는 코드를 읽어야 알 수 있으면 안 된다.

2. **좌표를 자리 기준 비율로 바꿔서 보낸다.** ⬅️ **규제 대화가 훨씬 쉬워진다**
   화면 좌표(이 사람이 방 안 어디 있었나) 대신 **이 자리의 몇 %를 덮었나**로 바꾼다.
   - **방 안 어디였는지는 복원이 안 된다.**
   - **판정을 배우는 데 필요한 것은 그대로 다 남는다** (5절의 세로÷가로, 짐 걸친 비율 등은 전부 비율이다).
   - 비용 0. 박스에서 나눗셈 한 번이다.

3. **두 종류로 나눠 보낸다.**

   | 종류 | 내용 | 크기 | 쓰임 |
   |---|---|---|---|
   | 매일 요약 | 자리별·시간대별 모름 사유 분포, IGNORE 비율 | 아주 작다 | **어느 매장 어느 자리가 문제인지** 바로 보인다 |
   | 어려운 사례만 | 모름이 난 틱, 순간 판정과 확정 판정이 어긋난 틱 | 전체의 일부 | **실제 학습 자료** (3절 1번) |

   잘 도는 97% 는 보낼 필요가 없다. **틀린 것만 있으면 된다.**

4. **전용 표에 묶어서(batch) 넣는다. 틱마다 넣지 않는다.**
   밤에 한 번, 그날 고른 것만 한 번에 `insert` 한다.
   **끊겨도 죽지 않게:** 로컬에 쓰고, 들어간 게 확인되면 지운다. 이미 그 모양이다 (끊김 시험 통과 기록).
   **스키마는 10절.**

### 이건 약속을 바꾸는 일이다

②를 하면 **사람 위치가(비율로 바뀌더라도) 매장을 떠난다.**
지금 안내문의 "밖으로 나가는 건 빈자리 숫자뿐" 은 더 이상 사실이 아니게 된다.
**안내문(`문큐연락/안내문시안/`)과 규제 샌드박스 신청서에 반영해야 한다.**
2번(비율로 바꾸기)을 하면 설명이 훨씬 쉬워진다: *"누가 어디 있었는지가 아니라, 자리가 얼마나 덮였는지만 나갑니다."*


---

## 10. 무엇을 남겨야 4절의 문제가 풀리나 — 표 설계

9절이 "보낸다" 는 결정이라면, 10절은 **"무엇을 보내야 쓸모가 있나"** 다.
기준은 하나: **4절의 문제 하나하나를 실제로 풀 수 있는 칸만 남긴다.**
"나중에 쓸지도 모르니까" 로 칸을 늘리지 않는다. 그게 개인정보 노출이고 용량이다.

### 10-1. 문제 → 필요한 칸 (이 표가 설계의 근거다)

| 풀려는 문제 | 반드시 있어야 하는 것 | 지금 있나 |
|---|---|---|
| **4-1 장식물 오인** | 물건 이름 · **자리 안에서의 위치와 크기(비율)** · **그때 영업중이었나** | ❌ 영업 여부 없음, 위치는 화면 좌표 |
| **4-2 짐을 버림** | **버려진 물건도 기록** + 버린 이유 + 그때의 면적 비율 | ❌ **버린 건 아예 안 남는다** |
| **4-3 못 찾음 (2번 길)** | **5장 중 몇 장에서 보였나** | ❌ 다수결로 합쳐진 뒤라 사라진다 |
| **4-3 못 찾음 (설치 문제 구분)** | 자리가 화면 가장자리에 얼마나 붙었나 | △ `border_cropped` 사유만 있음 |
| **4-3 못 찾음 (확신도 문제)** | 문턱 아래라 버린 탐지의 확신도 | △ `--log-detections` 켜야 남음 |
| **4-4 지갑 / 이름 없는 물건** | "빈 상태와 얼마나 달라졌나" 값 | ❌ 아직 그런 신호가 없다 |
| **5절 1.75 선 다시 긋기** | 세로÷가로 · 잰 각도들 · 못 잰 이유 | ✅ 있다 |
| **3-1 공짜 정답** | 순간 판정 · 확정 판정 · **틱 순서** | ✅ 있다 |
| **3-3 손님 피드백 붙이기** | 매장·자리·시각을 묶는 열쇠 | ✅ 있다 |
| **무엇과 비교하는지** | 모델·해상도·설정 지문, 배치도 판 | ❌ 로그 줄에 없다 |

**❌ 가 여섯 개다. 이걸 만드는 게 일의 대부분이다.** 보내는 건 그다음이다.

### 10-2. 표 넷

이름은 `seatnow_` 접두사로 앱 표와 확실히 갈라 둔다.

> **만드는 순서: ③ `seatnow_runs` 를 먼저, 그다음 ①.** ① 이 ③ 을 참조한다.
> 순서를 바꾸면 `relation "seatnow_runs" does not exist` 로 막힌다.

#### ① `seatnow_seat_ticks` — 학습 자료 본체

**한 줄 = 한 자리의 한 순간.** 전부 넣지 않고 10-3 규칙으로 **고른 것만** 넣는다.

| 칸 | 뜻 | 왜 필요한가 |
|---|---|---|
| `cafe_id` · `seat_id` · `tick_at` | 어느 매장 어느 자리 언제 | 열쇠. 손님 피드백을 붙일 때도 이걸로 |
| `run_id` | 어떤 설정으로 돌 때였나 | ③ 과 연결. **이게 없으면 지난주와 비교 불가** |
| `seat_kind` · `capacity` | 테이블인지 바 자리인지, 몇 명 | 바 자리는 규칙이 다르다 |
| `raw_state` | 그 순간 본 그대로 | 3-1 공짜 정답의 왼쪽 |
| `settled_state` | 흔들림 걸러낸 값 | 3-1 공짜 정답의 오른쪽 |
| `shown_state` | 앱에 실제로 나간 값 | 손님이 본 것과 대조 |
| `reason_code` | 모름이면 왜 | 개선 백로그 그 자체 |
| `votes_seen` / `votes_total` | **5장 중 몇 장에서 사람·짐이 보였나** | **4-3(2) 의 전부.** 지금 사라지는 값 |
| `is_open` | 그때 영업중이었나 | **4-1(b) 장식 지도의 전부** |
| `border_margin` | 자리가 화면 가장자리에서 얼마나 떨어졌나 (0~1) | 설치 결함과 코드 결함을 가른다 |
| `novelty` | 빈 상태와 얼마나 달라졌나 (0~1) | **4-4 지갑까지 커버하는 유일한 칸** |
| `persons` (jsonb) | 사람마다 `{hw_ratio, angles, pose_reason, conf, cover}` | 5절의 1.75 선을 다시 긋는 재료 |
| `objects` (jsonb) | 물건마다 `{class, conf, share, cx, cy, w, h, kept, drop_reason}` | **`kept=false` 가 4-2 의 전부** |
| `chairs_linked` | 붙은 의자 수 | 5절 규칙 2 |

> **`persons`·`objects` 안의 위치는 전부 "그 자리를 1로 봤을 때의 비율" 이다.**
> 화면 좌표는 넣지 않는다 (9절 2번). 방 안 어디였는지 복원이 안 되고,
> 판정을 배우는 데는 비율이면 충분하다.

```sql
create table seatnow_seat_ticks (
  id           bigserial primary key,
  cafe_id      text        not null,
  seat_id      text        not null,
  tick_at      timestamptz not null,
  run_id       uuid        not null references seatnow_runs(run_id),
  seat_kind    text        not null,
  capacity     smallint,
  raw_state    text        not null,   -- occupied | empty | unknown | ignore
  settled_state text       not null,
  shown_state  text,
  reason_code  text,
  votes_seen   smallint    not null,
  votes_total  smallint    not null,
  is_open      boolean     not null,
  border_margin real,                  -- 0 = 화면 끝에 붙음
  novelty      real,                   -- 0 = 빈 상태와 같음
  chairs_linked smallint,
  persons      jsonb       not null default '[]',
  objects      jsonb       not null default '[]',
  sample_kind  text        not null    -- hard | control | transition
);
create index on seatnow_seat_ticks (cafe_id, tick_at desc);
create index on seatnow_seat_ticks (reason_code) where reason_code is not null;
```

#### ② `seatnow_seat_daily` — 요약, 영구 보관

**한 줄 = 한 자리의 하루.** 아주 작다. **①을 지워도 이건 남긴다.**
"어느 매장 어느 자리가 문제인가" 는 이것만 봐도 답이 나온다.

| 칸 | 뜻 |
|---|---|
| `cafe_id` · `seat_id` · `day` | 열쇠 |
| `ticks` | 그날 판단 횟수 |
| `occupied` · `empty` · `unknown` · `ignore` | 상태별 횟수 |
| `reason_counts` (jsonb) | 모름 사유별 횟수 ← **다음 개선 백로그** |
| `unknown_rate` · `ignore_rate` | 엔지니어링 지표 / **설치 품질 지표** (`CLAUDE.md`) |

```sql
create table seatnow_seat_daily (
  cafe_id text, seat_id text, day date,
  ticks int, occupied int, empty int, unknown int, ignore_ int,
  reason_counts jsonb not null default '{}',
  unknown_rate real, ignore_rate real,
  primary key (cafe_id, seat_id, day)
);
```

#### ③ `seatnow_runs` — 무엇과 비교하는지

**한 줄 = 박스가 한 번 켜져서 도는 동안.** 아주 작다.
**이게 없으면 "이번 주가 지난주보다 나아졌다" 를 말할 수 없다.**
설정이 바뀐 걸 모르고 비교하면 전부 헛일이다.

담을 것: 박스 소프트웨어 판 · 모델 이름과 해상도 · 판단 주기 · 5장 규칙 값 ·
**판정 설정 전체의 지문(해시)** · 배치도 판 번호 · 시작·종료 시각.

```sql
create table seatnow_runs (
  run_id uuid primary key,
  cafe_id text not null,
  started_at timestamptz not null, ended_at timestamptz,
  box_version text, det_model text, pose_model text, imgsz int,
  tick_seconds real, median_frames smallint,
  settings_hash text not null,   -- AnalysisSettings 전체의 지문
  layout_version text
);
```

#### ④ `seatnow_scenery` — 장식 지도

**한 줄 = 한 매장의 하루치 "원래 거기 있는 것" 목록.**
**박스가 문 닫은 시간에 스스로 만든다.** 원본 화면은 안 올라가고 결과만 올라간다.

담을 것: 자리마다 `{class, cx, cy, w, h, 며칠째 그 자리에 있나}`.
**영업중·문 닫은 시간 둘 다에서 보인 것만** 장식으로 올린다 (4-1(b) 의 주의).

### 10-3. 무엇을 고를 것인가 — 이게 핵심 설계다

전부 보내면 한 매장 하루 2만 3천 줄이다. **틀린 것만 있으면 된다.**

**남길 것 (`sample_kind = hard`):**
- 모름이 난 자리
- 순간 판정과 확정 판정이 **어긋난** 자리
- **5장 투표가 만장일치가 아닌** 자리 (4-1, 3-2 처럼 갈린 것)
- **물건을 규칙으로 버린** 자리 (4-2)
- 문턱 아래 탐지가 근처에 있던 자리 (4-3)

**전이 (`transition`):** 자리 상태가 실제로 바뀐 순간. 드물고 제일 비싸다. 무조건 남긴다.

**대조군 (`control`): 잘 돌아간 것 중 1% 를 무작위로 남긴다.** ⬅️ **빼먹기 쉬운데 필수다**
어려운 것만 모으면 **그 데이터로 배운 규칙이 편향된다.** 잘 되는 경우가 어떻게 생겼는지
모르면 "모름을 줄였다" 가 "멀쩡한 걸 망쳤다" 인지 구분할 수 없다.

**양 추정** (자리 7개, 15초 주기, 하루 14시간 영업 기준):

| | 하루 | 30일 | 매장 10곳 30일 |
|---|---:|---:|---:|
| 전부 | 23,500줄 | 70만 | 700만 |
| **고른 것 (약 10%)** | **2,400줄** | **7만** | **70만** |

**보관:** ① 은 90일, 그 뒤 ② 로 접고 지운다. ②③④ 는 남긴다.

### 10-4. 나가지 않는 것 (금지 목록)

이 목록을 코드 한 곳에 두고, **여기 없는 칸이 생기면 시험이 깨지게 한다** (9절 1번).

- **화면 좌표.** 전부 자리 기준 비율로 바꾼 뒤 보낸다.
- **사진·영상 일체.** 어떤 형태로도.
- **사람을 구분하는 값.** 추적 번호도 하루를 넘겨 이어지면 안 된다.
- **자유 문자열 사유.** 정해둔 사유 코드만. 디버그 문자열에 좌표가 섞여 나간 전례가 있다.

**RLS:** 네 표 모두 RLS 를 켜고 **박스 계정만 `insert`**, 앱 쪽 `anon` 은 **읽기 없음**.
같은 프로젝트를 쓰므로 이걸 안 하면 손님 앱 키로 사람 위치 기록이 읽힌다.

### 10-5. 구현 상태 (2026-09-11)

| 10-1 의 ❌ | 상태 |
|---|---|
| 5장 투표 결과 | ✅ **이미 있었다.** 엔진이 `vote_counts` 를 남기고 있다. 읽어 쓰면 됐다 |
| 영업 여부 | ✅ `--open-hours "09:00-22:00"` |
| 자리 기준 비율 좌표 | ✅ 나가는 모든 위치값에 적용 |
| 설정 지문 | ✅ `seatnow_runs.settings_hash` |
| 화면 가장자리까지 거리 | ✅ `border_margin` |
| **버린 물건** | ❌ **아직.** 아래 참조 |
| `novelty` (평소와 다른 것) | ❌ 아직. 칸만 만들어 뒀다 |

**만든 것**

| 파일 | 하는 일 |
|---|---|
| `deploy/supabase/telemetry.sql` | 표 넷 + 접근 제한. 대시보드에 붙여넣어 한 번 실행 |
| `edge/telemetry.py` | 로그 한 줄 → 표의 줄. 고르는 규칙(10-3)까지. **순수 파이썬** |
| `edge/telemetry_spool.py` | 디스크에 쌓기. 끊겨도 한 줄도 안 잃는다 |
| `edge/telemetry_upload.py` | 하루 한 번 올리는 명령 |
| `tests/test_telemetry*.py` | 35개. 카메라·모델 없이 돈다 |

**켜는 법** (`deploy/seatnow.env`):

```
SEATNOW_EXTRA=--telemetry-dir /home/hugo/seatnow/results/live/telemetry --open-hours 11:00-17:30
```

문큐는 **11:00~17:30** 이다 (2026-09-11 확인). 매장마다 다르니 설치할 때 확인하고 적는다.

**보내는 법** (하루 한 번, systemd timer 나 cron):

```
python3 -m edge.telemetry_upload --dry-run   # 몇 줄 밀렸는지만
python3 -m edge.telemetry_upload             # 어제까지 올린다
```

판정 루프는 **파일에 덧붙이기만** 한다. 전송은 완전히 따로 돈다 —
7.5초 예산에 전송이 끼면 안 된다 (`CLAUDE.md`).

### 10-6. 남은 것

1. **버린 물건을 남긴다 (4-2).** 자리 배정에서 규칙에 걸린 물건이 그냥 사라진다
   (`engine/seatnow_core.py` 의 `_object_table_score` 가 0을 돌려주고 끝).
   → 버린 후보를 이유와 함께 관측에 달고, `track_to_dict` 에 실으면 된다.
   받는 쪽(`object_rows`)은 **이미 `kept`/`drop_reason` 을 읽게 되어 있다.**
2. **`novelty` (4-3(4), 4-4).** 제일 크고 마지막. **이것만 판정에 영향을 준다** →
   72칸 정답지로 채점한다. 나머지는 전부 순수 기록 추가라 재채점이 필요 없다.
