# 다음 할 일 — 여기서부터 읽으면 된다

> 갱신: 2026-09-07. 이 파일은 "지금 어디까지 왔고, 다음에 뭘 하나"만 적는다.
> 방법·명령은 각 문서를 가리킨다. 순서대로 하면 된다.

## 지금 어디까지 왔나

| 끝난 것 | 근거 |
|---|---|
| 엣지 박스에서 판정이 돈다 (파일 + **실시간 카메라 입력**) | `docs/edge-setup.md` 7·11단계 |
| 판정 정확도는 노트북과 동일 (72칸 중 70) | 8단계 |
| 카메라 사양 확정: **4MP · H.265 · 2.8mm 렌즈** | 9단계 + 11단계 실측 |
| 박스가 꺼졌다 켜져도 알아서 다시 돈다 (자동 실행) | 12단계, 재부팅 시험 통과 |
| **26시간 무인 운영 통과** — 판정 6,366번, 공백 0, 메모리 안 늘어남 | 2026-09-07 확인 |
| 살 것 정리 | `구매목록.md` |

**박스는 지금 꺼도 된다.** 다음에 켜면 전원만 들어오면 판정이 다시 시작된다.
저장할 것도, 종료 전에 할 것도 없다.

## 박스 끄고 켜기

- **끄기:** 노트북에서 `ssh hugo@192.168.0.39` 로 붙어서 `sudo poweroff`
  (비밀번호 묻는다). 또는 박스 전원 버튼을 **짧게 한 번** 누른다 — 우분투가
  정상 종료한다. 전원을 그냥 뽑아도 망가지진 않지만 위 둘이 낫다.
- **켜기:** 전원 버튼. 모니터에 로그인 창이 떠도 **로그인하지 않아도 된다.**
  판정은 그 뒤에서 이미 돈다. 1분쯤 뒤 노트북에서 확인:
  `ssh hugo@192.168.0.39 "systemctl --user status seatnow | grep Active"`

## 1. 사기 (지금)

`구매목록.md` 를 그대로 본다. 요약:

| 무엇 | 고른 것 | 값 |
|---|---|---:|
| 카메라 | 하이크비전 **DS-2CD1143G2-LIU 2.8mm** (네이버 알온테크) | 68,000원 |
| PoE 인젝터 | TL-POE150S 품절 시 **유비큐넷 PSE801FM** | 13,050원 |
| 랜선 | 가진 CAT.5e 28AWG 를 천장용으로 써도 된다 (30m 닿으면). 짧은 것 2개는 다이소 3m | 0~6,000원 |

사기 전에 잴 것: 인젝터→천장 카메라 자리까지 선이 지나갈 길이, 공유기 빈
랜포트 2개, 인젝터 꽂을 콘센트.

## 2. 카메라가 오기 전에 (선택 — 카메라 없이 되는 개발)

시간이 있으면 이걸 한다. 없으면 3번으로 건너뛴다.

- **판정 결과를 보여줄 서버·앱.** 지금은 판정이 박스 안 파일
  (`results/live/날짜.jsonl`)에만 쌓이고 **아무도 못 본다.** 손님이 보는 화면과
  그걸 먹이는 서버가 다음 큰 덩어리다. 박스 안 가짜 카메라가 계속 결과를
  만들어주므로 지금 붙여 볼 수 있다. 판정 공백(`gap` 줄)은 앱에서 **모름**으로
  보여야 한다 — 옛 값을 유지하면 안 된다 (`CLAUDE.md` 3상태 원칙).
- **정확도 루프.** 72칸 정답지로 채점하고 UNKNOWN 사유별로 코드로 고칠 수
  있는 것을 고친다 (`goal` 스킬, `plan.md` §1·§5).

## 3. 카메라가 오면 — 설치 당일 순서

전부 `구매목록.md` "설치 당일 반드시 할 것" 과 `docs/edge-setup.md` 12단계에 있다. 순서만:

1. **공유기에서 IP 예약.** 카메라와 박스 둘 다. 안 하면 재부팅 뒤 주소가
   바뀌어 판정이 조용히 멈춘다.
2. **카메라 설정 화면.** RTSP 켜기 · 관리자 비밀번호 · 메인 스트림
   **2560×1440 · H.265 · 15~20fps**.
3. **연결.** 공유기 → 인젝터(LAN IN) · 인젝터(POE OUT) → 카메라 · 공유기 → 박스.
   인젝터 상자에 전원 어댑터가 들어 있다. (`구매목록.md` "연결 방법")
4. **달고 화각 잡기.** 천장에 임시로 붙이고 카메라에서 10초 클립을 떠서
   좌석이 다 들어오는지 본다. 안 들어오는 좌석이 있으면 위치를 옮긴다 —
   화면 밖 좌석은 설치 결함이다 (`CLAUDE.md`).
5. **좌석 상자 그리기.** 그 클립으로 `install/calibrate.py`. 자동으로 잡아준
   것에서 잘못된 것 지우고 못 잡은 것(일자형 책상·벽에 붙은 책상) 추가. 1회로 끝.
6. **박스에 주소 넣기.** `deploy/seatnow.env` 의 `SEATNOW_URL` 과
   `SEATNOW_LAYOUT` 을 바꾸고 `systemctl --user restart seatnow`.
   가짜 카메라는 끈다: `systemctl --user disable --now seatnow-fake-camera`.
7. **확인.** `journalctl --user -u seatnow -n 5 -o cat` 에 판정 줄이 15초마다
   찍히면 된다. 시작 화면에 `하드웨어 디코딩 켜짐 (vaapi)` 가 있어야 한다.
8. **하루 뒤.** `./venv/bin/python -m edge.live_report results/live/날짜.jsonl`
   로 틱 시간·공백·재연결 횟수를 본다. 진짜 카메라에서 처음 보는 숫자다 —
   재연결이 잦으면 11단계 "진짜 카메라를 달면 다시 볼 것".

## 문서 지도

| 알고 싶은 것 | 어디 |
|---|---|
| 박스에 붙는 법, 단계별 명령, 막혔을 때 | `docs/edge-setup.md` (맨 위 "먼저" → 12단계) |
| 뭘 사나, 왜 그 사양인가, 사면 안 되는 것 | `구매목록.md` |
| 결정 기록과 남은 작업 목록 | `plan.md` |
| 운영 원칙 (카메라 1대, 3상태, 수작업은 설치 1회) | `CLAUDE.md` |
