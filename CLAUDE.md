# CLAUDE.md — dwnc_cafe_cctv (SeatNow)

프로젝트 배경·판정 로직은 `SEATNOW_전체정리.md`, 작업 플랜은 `plan.md` 참조.
여기에는 **코드만 봐서는 알 수 없는 운영 전제**만 적는다.

## 카메라는 우리가 고르고 우리가 설치한다

카메라의 기종·위치·각도·높이는 **SeatNow 팀이 판단하고 직접 설치한다.**
매장(사장님)에게 설치나 위치 선정을 맡기지 않는다.

이 전제에서 따라오는 규칙들:

- **화각이 못 보는 좌석(`IGNORE`)은 제품의 한계가 아니라 설치 결함이다.**
  IGNORE 비율이 높으면 코드로 보정하려 들지 말고 **카메라를 다시 설치한다.**
  설치 검수의 합격 조건에 IGNORE 비율을 넣는다.
- 반대로 **`UNKNOWN`(보이는데 판정 실패)은 코드로 푼다.** 이 둘을 섞지 말 것.
  IGNORE는 설치 품질 지표, UNKNOWN은 엔지니어링 지표다.
- 각도가 극단적이어서 판정이 어려운 화각은 애초에 선택하지 않는다.
  "어떤 각도에서도 동작하는 알고리즘"보다 **"좋은 각도를 고르는 설치 기준"**이 먼저다.
- 설치 기준(높이·부각·화각·좌석까지 거리)이 정해지면 이 파일에 추가한다.

### 카메라는 매장당 1대다

**매장당 카메라 1대**를 목표로 개발한다 (`plan.md` §2 결정 기록).
2대로 가림을 푸는 선택지는 없다고 보고 설계한다 — 1대로 커버되는 화각을
고르는 것, 그리고 가려진 좌석을 구제 경로로 살리는 것이 우리가 푸는 문제다.

- 좌석 배치를 1대로 못 덮으면 **좌석을 포기(IGNORE)하거나 카메라 위치를 다시 고른다.**
  2대를 전제한 기능(뷰 간 좌석 매칭, 다중 스트림 동기화 등)은 만들지 않는다.
- 1대 전제 때문에 **가림이 상시적이다.** 하반신이 테이블에 가려 각도 측정이
  안 되는 경우가 기본값이므로, 의자 연결·짐 증거 같은 구제 경로가 부가 기능이
  아니라 주 경로다 (`plan.md` T1의 회귀가 치명적이었던 이유).
- 재검토 조건: 라벨링된 평가셋에서 IGNORE 비율이 감당 못 할 수준으로 나올 때만.

## 매장에 요구할 수 있는 수작업은 "설치 시 1회 검수"가 전부다

설치 당일 `install/calibrate.py`로 한 번 확인하고 끝낸다. 그 이상은 요구하지 않는다.

- 허용: `_preseed`가 자동으로 잡아준 결과에서 **잘못 잡힌 것 지우기 + 못 잡은 것 추가하기**
  (특히 일자형 책상·벽에 붙은 책상은 모델이 절대 못 잡으므로 사람이 그려야 한다)
- 금지: 매장별·각도별 **임계값 튜닝을 요구하는 설계**.
  `AnalysisSettings`(`engine/seatnow_core.py:156-185`)의 기본값 하나로 굴러가야 한다.
- 금지: 운영 중 주기적인 재캘리브레이션을 전제한 설계.

## 판정 결과는 3상태로 정직하게 낸다

애매한 것을 `empty`로 반올림하지 않는다. `occupied` / `empty` / `unknown`을 그대로 내보내고,
**UNKNOWN 비율을 줄이는 것을 개발의 방향**으로 삼는다.

- `totals.free`는 **확신하는 empty만** 센다.
- UNKNOWN에는 사유 코드를 붙인다. 사유별 분포가 곧 다음 개선 백로그다.
  (기존 어휘: `compact_occluded_pose`, `insufficient_keypoints`,
  `nearby_person_pose_unknown`, `no_customer_evidence`, `border_cropped`)

## 설명은 비개발자 기준으로 한다

이 저장소의 사용자는 개발자가 아니다. 코드를 읽지 않아도 판단할 수 있게 설명한다.

- **함수명·파일명·영어 약어를 설명 없이 쓰지 않는다.** 근거로 파일:줄번호를 다는 건
  좋지만, 그게 설명의 본문이 되면 안 된다.
- 기술 용어가 꼭 필요하면 **처음 나올 때 한 줄로 풀어준다.**
  예: "UNKNOWN = 보이긴 하는데 사람이 앉은 건지 아닌지 판단이 안 되는 상태"
- **결정을 물을 때는 매장과 손님 입장의 상황으로 설명한다.**
  "스키마를 어떻게 할까요"가 아니라 "손님 앱에 뭐라고 뜨게 할까요"로 묻는다.
  고르면 실제로 무엇이 달라지는지, 잘못 고르면 어떤 일이 벌어지는지를 같이 말한다.
- **트레이드오프를 숨기지 않는다.** 우리가 고른 방식의 단점도 같은 무게로 말한다.
  특히 그 단점이 손님에게 잘못된 정보를 주는 방향이면 반드시 먼저 알린다.

## 개인정보

- 배포 시 `--no-video`가 기본값이다. 주석 MP4를 디스크에 쓰면
  "영상 미저장" PIPA 예외 조항이 깨진다 (`plan.md` T10).
