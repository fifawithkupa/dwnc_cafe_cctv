# 다음 할 일 — 여기서부터 읽으면 된다

> 갱신: 2026-09-08. 이 파일은 "지금 어디까지 왔고, 다음에 뭘 하나"만 적는다.
> 방법·명령은 각 문서를 가리킨다. 순서대로 하면 된다.

## ✅ 2026-09-08 — 카메라·인젝터 도착, 집에서 연결 시험 통과

**카메라가 박스에 붙어서 돈다.** 좌석은 아직 없으니(집에서 시험) 판정 내용은 무시하고 연결만 봤다.

| 한 것 | 값 |
|---|---|
| 카메라 활성화 · 관리자 비밀번호 | 됨. 비밀번호는 박스 `deploy/seatnow.env` 에만 있다 (저장소에 없음) |
| 카메라 주소 | **고정 IP 192.168.0.50** (게이트웨이 192.168.0.1). 공장값 192.168.1.64 에서 옮김 |
| 메인 스트림 | **H.265 · 2560×1440 · 20fps** (박스 ffprobe 로 확인) |
| 시계 | 한국 시간대 + NTP (처음엔 1970년 중국 시간대였다) |
| 화면 글자(날짜·Camera 01) | 껐다. 좌석을 가릴 수 있어서 |
| 박스 서비스 | 가짜 카메라 끄고 진짜 카메라로 재시작. **하드웨어 디코딩 켜짐 (vaapi)**, 틱 15초, 추론 3.3~3.4초 |

**주소를 옮기는 법 (다음 매장에서도 같다).** 새 하이크비전은 192.168.1.64 로 켜지고
공유기 대역이 아니라 아무도 못 붙는다. 노트북 설정은 건드리지 않는다(`CLAUDE.md`).
박스에 임시 주소를 붙이고(`sudo ip addr add 192.168.1.100/24 dev enp0s31f6`, 재부팅하면
사라짐) 박스를 거쳐 카메라 설정 화면을 노트북으로 끌어온다
(`ssh -L 8080:192.168.1.64:80 hugo@박스IP` → 브라우저 `http://127.0.0.1:8080`).
활성화 뒤 나머지 설정(H.265·시간·주소)은 카메라의 ISAPI(웹 설정 API)로 바꿨다.
주소 변경은 **재부팅해야 적용**된다 (응답에 `rebootRequired`).

**2026-09-08 저녁, 카페 가기 전 준비 끝.** 카메라는 **자동 주소 받기(DHCP)** 로 바꿨다
(`카페설치당일.md` 0-3). 집에서는 192.168.0.40 을 받았고 박스 설정도 그걸로 맞춰 계속
돈다. 카페에서는 "카메라 찾아줘" 로 새 주소를 찾아 그 대역의 고정 주소로 바꾼다.
찾을 때는 대역 전체를 한 번 훑어야 한다 — 카메라는 브로드캐스트 핑에 답하지 않는다.
자동 실행(enabled)·재부팅 뒤 자동 로그인(Linger=yes) 확인했다.

**남은 것 (카페 가기 전, 집에서).** Supabase 를 실제로 한 번 켜 본다. 표·박스 계정·
`boxes` 줄은 됐다 (2026-09-10, 집 시험은 앱의 비공개 시험 카페 `test-001` 로). 남은 건
박스 `seatnow.env` 에 다섯 값(비밀번호는 사용자에게) → 재시작 → 카메라 선을 뽑았다 꽂아
`gap` 과 복구가 그대로 오는지 확인. 이걸 안 하고 카페에 가면 현장에서 처음 시험하게 된다.

**남은 것 (매장 설치 당일).** 아래 3번의 1·4·5·6번만 남았다:
공유기에서 **박스** IP 예약(카메라는 고정 IP 라 필요 없음) → 천장에 달고 화각 확인 →
좌석 상자 그리기 → `SEATNOW_LAYOUT` 바꾸고 재시작.
**내일** `./venv/bin/python -m edge.live_report results/live/2026-09-08.jsonl` 로 진짜
카메라의 재연결 횟수를 본다.

## 지금 어디까지 왔나

| 끝난 것 | 근거 |
|---|---|
| 엣지 박스에서 판정이 돈다 (파일 + **실시간 카메라 입력**) | `docs/edge-setup.md` 7·11단계 |
| 판정 정확도는 노트북과 동일 (72칸 중 70) | 8단계 |
| 카메라 사양 확정: **4MP · H.265 · 2.8mm 렌즈** | 9단계 + 11단계 실측 |
| 박스가 꺼졌다 켜져도 알아서 다시 돈다 (자동 실행) | 12단계, 재부팅 시험 통과 |
| **26시간 무인 운영 통과** — 판정 6,366번, 공백 0, 메모리 안 늘어남 | 2026-09-07 확인 |
| 살 것 정리 | `구매목록.md` |

**박스는 지금 꺼도 된다.** 다음에 켜면 전원만 들어오면 판정이 다시 시작된다.
저장할 것도, 종료 전에 할 것도 없다.

## 박스 끄고 켜기

- **끄기:** 노트북에서 `ssh hugo@192.168.0.39` 로 붙어서 `sudo poweroff`
  (비밀번호 묻는다). 또는 박스 전원 버튼을 **짧게 한 번** 누른다 — 우분투가
  정상 종료한다. 전원을 그냥 뽑아도 망가지진 않지만 위 둘이 낫다.
- **켜기:** 전원 버튼. 모니터에 로그인 창이 떠도 **로그인하지 않아도 된다.**
  판정은 그 뒤에서 이미 돈다. 1분쯤 뒤 노트북에서 확인:
  `ssh hugo@192.168.0.39 "systemctl --user status seatnow | grep Active"`

## 1. 사기 (지금)

`구매목록.md` 를 그대로 본다. 요약:

| 무엇 | 고른 것 | 값 |
|---|---|---:|
| 카메라 | 하이크비전 **DS-2CD1143G2-LIU 2.8mm** (네이버 알온테크) | 68,000원 |
| PoE 인젝터 | TL-POE150S 품절 시 **유비큐넷 PSE801FM** | 13,050원 |
| 랜선 | 가진 CAT.5e 28AWG 를 천장용으로 써도 된다 (30m 닿으면). 짧은 것 2개는 다이소 3m | 0~6,000원 |

사기 전에 잴 것: 인젝터→천장 카메라 자리까지 선이 지나갈 길이, 공유기 빈
랜포트 2개, 인젝터 꽂을 콘센트.

## 2. 카메라가 오기 전에 (선택 — 카메라 없이 되는 개발)

시간이 있으면 이걸 한다. 없으면 3번으로 건너뛴다.

- **판정 결과 전송 — 됐다 (2026-09-08).** 박스가 15초마다 Supabase `cafe_live` 에 쓰고,
  손님 지도는 `cafe_maps` 에 올린다. 앱 개발자는 `docs/앱연동.md` 를 본다. 설계는
  `docs/superpowers/specs/2026-09-08-supabase-publish-design.md`.
  **2026-09-10:** 실제 프로젝트를 읽어보니 앱의 `cafes` 는 앱 자체 트리거가 채우는 표라
  박스가 쓰면 안 됐다 (혼잡도 칸은 우리 낱말을 거부). **박스는 `cafe_live`·`cafe_maps` 에만
  쓰기로 결정**, 앱은 `cafe_live` 를 읽는다. 표·계정·`boxes` 줄은 됐고, 남은 건 박스
  `seatnow.env` 다섯 값 → 재시작 → 끊김 시험. 문큐 카페 줄은 앱 개발자가 넣는다 (`plan.md` §8-b).
- **정확도 루프.** 72칸 정답지로 채점하고 UNKNOWN 사유별로 코드로 고칠 수
  있는 것을 고친다 (`goal` 스킬, `plan.md` §1·§5).

## 3. 카메라가 오면 — 설치 당일 순서

**현장에서는 `카페설치당일.md` 를 본다.** 챙길 것, 천장 종류별 고정 방법, 각 단계에서
Claude 에게 할 말까지 다 있다. 아래는 요약이다:

1. **공유기에서 IP 예약.** 카메라와 박스 둘 다. 안 하면 재부팅 뒤 주소가
   바뀌어 판정이 조용히 멈춘다.
2. **카메라 설정 화면.** RTSP 켜기 · 관리자 비밀번호 · 메인 스트림
   **2560×1440 · H.265 · 15~20fps**.
3. **연결.** 공유기 → 인젝터(LAN IN) · 인젝터(POE OUT) → 카메라 · 공유기 → 박스.
   인젝터 상자에 전원 어댑터가 들어 있다. (`구매목록.md` "연결 방법")
4. **달고 화각 잡기.** 천장에 임시로 붙이고 카메라에서 10초 클립을 떠서
   좌석이 다 들어오는지 본다. 안 들어오는 좌석이 있으면 위치를 옮긴다 —
   화면 밖 좌석은 설치 결함이다 (`CLAUDE.md`).
5. **좌석 상자 그리기.** 그 클립으로 `install/calibrate.py`. 자동으로 잡아준
   것에서 잘못된 것 지우고 못 잡은 것(일자형 책상·벽에 붙은 책상) 추가. 1회로 끝.
6. **박스에 주소 넣기.** `deploy/seatnow.env` 의 `SEATNOW_URL` 과
   `SEATNOW_LAYOUT` 을 바꾸고 `systemctl --user restart seatnow`.
   가짜 카메라는 끈다: `systemctl --user disable --now seatnow-fake-camera`.
7. **확인.** `journalctl --user -u seatnow -n 5 -o cat` 에 판정 줄이 15초마다
   찍히면 된다. 시작 화면에 `하드웨어 디코딩 켜짐 (vaapi)` 가 있어야 한다.
8. **하루 뒤.** `./venv/bin/python -m edge.live_report results/live/날짜.jsonl`
   로 틱 시간·공백·재연결 횟수를 본다. 진짜 카메라에서 처음 보는 숫자다 —
   재연결이 잦으면 11단계 "진짜 카메라를 달면 다시 볼 것".

## 문서 지도

| 알고 싶은 것 | 어디 |
|---|---|
| 박스에 붙는 법, 단계별 명령, 막혔을 때 | `docs/edge-setup.md` (맨 위 "먼저" → 12단계) |
| 뭘 사나, 왜 그 사양인가, 사면 안 되는 것 | `구매목록.md` |
| **카페 설치 당일** — 챙길 것, 천장별 고정, Claude 에게 할 말 | `카페설치당일.md` |
| 결정 기록과 남은 작업 목록 | `plan.md` |
| 운영 원칙 (카메라 1대, 3상태, 수작업은 설치 1회) | `CLAUDE.md` |
