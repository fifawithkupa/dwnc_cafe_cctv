# SeatNow 코드 작업 플랜

> 갱신: 2026-09-02 — 정답지 루프 한 바퀴 완료(§1). 끝난 연대기는 §7로 압축했다.
> 먼저 읽을 것: **`CLAUDE.md`** (카메라 설치 주체, 수작업 예산, 3상태 출력 원칙)
> 배경·판정 로직 전체는 `SEATNOW_전체정리.md`
>
> ## ⬅️ **내일 할 일: 엣지 박스 첫 실행** — `docs/edge-first-run.md`
> OptiPlex 7040 Micro / i3-6100T / RAM 4GB / Linux.
> 얻을 숫자는 둘 — **`inference=?ms`(합격선 7.5초)** 와 **`accepted=70`**.
> 그 두 숫자가 나와야 카메라 선정도 파인튜닝 여부도 그다음으로 간다.

---

## 0. 지금 어디까지 왔나

| | 상태 |
|---|---|
| 판정 엔진 | 동작. **정답지 72칸 중 70칸이 옳다** (§1) |
| 평가 방법 | **72칸 정답지로 자동 채점한다** (§1). 라벨링 평가셋은 아직 없다 |
| 영상 입력 | 로컬 파일 전용. RTSP 없음 (T8) |
| 엣지 하드웨어 | **OptiPlex 7040 Micro (RAM 4GB) 확보.** 첫 실행 절차는 `docs/edge-first-run.md` |
| 카메라 | 미구매. 엣지 벤치 후 결정 (§2) |
| 평면도(손님용 지도) | 편집기 완성, 실제로 그리는 작업은 보류 |

**테스트 665개** — 모델·GPU 없이 순수 로직으로 돈다.
판정 로직을 고칠 때는 **실패 사례의 실제 좌표로 회귀 테스트를 추가**하는 것이 이 저장소의 관례다.

---

## 1. 지금 하던 일 — 검수·판정 개선 루프 ⬅️ **여기서 이어서 시작**

angle1 영상 하나를 놓고 **정답지로 채점 → 원인을 숫자로 찾음 → 고침 → 다시 돌림**을 반복한다.
2026-09-02 부터는 사장님이 준 **72칸 정답지**로 자동 채점한다 (§1-a 도구, `/goal` 스킬).

### 한 바퀴 도는 법

```bash
cd C:\Users\jin06\orca\workspaces\dwnc_cafe_cctv\main

# 1. 분석 실행 (~70초, CPU)
./venv/Scripts/python.exe -m engine.seatnow sample_raw/cafe_sample_angle1.mov \
  --layout layouts/cafe_angle1.json --log results/angle1_layout/log.jsonl \
  --frame-dir results/angle1_layout --log-detections --no-video

# 2. 사람이 볼 검수 폴더 다시 그리기 (영상 재실행 없음, 즉시)
./venv/Scripts/python.exe -m checks.make_review results/angle1_layout --title angle1

# 3. 판독표
./venv/Scripts/python.exe -m checks.inspect_run results/angle1_layout/log.jsonl \
  --judge results/angle1_layout/judge --output results/angle1_layout/report.md \
  --title "angle1 (사람이 그린 레이아웃, 15초 고정)"

# 4. 테스트
./venv/Scripts/python.exe -m unittest discover tests
```

그다음 `results/angle1_layout/review/` 를 탐색기로 열고 방향키로 넘긴다. 보는 법은 그 폴더의 `00_읽는법.md`.

### 정답지로 채점한 결과 (2026-09-02)

사장님이 6장 × 12자리 = **72칸 정답지**를 줬다 (`results/angle1_layout/angle_answer.md`).
루프를 한 바퀴 돌린 결과가 `results/angle1_layout/루프결과.md`.

| | 시작 | 끝 |
|---|---:|---:|
| 옳게 나온 칸 (72칸 중) | 67 | **70** |
| 고칠 칸 | 5 | **2** |
| 근거를 놓친 칸 | 13 | 10 |

**남은 2칸은 전부 탐지 문제다 — 코드로 고칠 것이 없다.**

남은 것은 두 종류이고 **고치는 방법이 다르다**:
① **이름을 틀린다** — 세워둔 노트북을 `chair` 0.20 으로 부른다 (T5 30·45초, T6 0·15초).
정확한 자리에서 매번 보는데 클래스가 가구라 통째로 버려진다.
② **아예 못 본다** — BAR7-6 의자 짐, 0.012~0.026.

①을 규칙으로 구해봤다가 70 → 69 로 내려가고 유령 의자가 생겨서 되돌렸다.
자세한 근거는 `루프결과.md`.

고친 것 셋: ① 좌석 상자가 그린 자리에서 흘러가던 것(이제 *이동량*만 따라감)
② 의자 위 짐의 크기 상한을 테이블과 같은 0.50 으로 ③ T3에 잘못 그려진 의자 삭제.

### 채점이 오답으로 세지 않는 것 (사장님 공지, 2026-09-02)

정답지는 **사람의 눈**이다. 다음 둘은 규칙이 일부러 그렇게 낸 것이라 맞는 판단이다.

- **가림 모름** — 사람이 자리를 덮었다. 비키면 풀린다
- **확정 대기** — 사용중↔빈자리 전이 중이다. 성급히 확정하면 손님이 남의 자리로 간다

점수를 올리려고 이 둘을 없애는 방향의 수정은 **금지**다.

### 다음 후보

1. **파인튜닝 판단** ⬅️ 이제 근거가 다 모였다. `루프결과.md` 의 표가 그대로
   학습 데이터 요구사항이다 — **세워둔 노트북·의자 위 가방을 '의자'로 부르지
   않게** + **바 끝의 작은 짐을 보게**
2. **`imgsz`·모델 크기 상향** — 파인튜닝보다 훨씬 싸다. **크롭 재탐지가 75초에
   같은 노트북을 `laptop` 으로 제대로 불렀다** — 해상도가 오르면 이름 문제가
   상당 부분 그냥 풀릴 수 있다는 직접 증거다. 엣지 박스가 오면 이 카드부터
3. **`sitting_angle` 재검토** — §2에서 "평가셋이 나온 뒤에"로 미뤄뒀는데,
   **이제 정답지가 생겨서 조건이 충족됐다.** 근거는 §5-b ①
4. **좌석 상자가 아직 조금씩 움직인다** — 75초에 최대 50픽셀. 카메라 미세
   흔들림을 따라가는 것이라 지금은 도움이 되지만, 몇 시간 단위로 무한히
   흘러가는지는 78초 영상으로 확인할 수 없다. **긴 영상이 생기면 반드시 볼 것**
5. T1·T2·T4·T6 상자 점검 — T3·T5는 이번에 봤다

### 사람 눈 정답표

`results/angle1_layout/angle_answer.md` — **72칸 전부 채워진 현재 정답지.**
`review/확인완료_2026-09-02/내가본 report.md` 는 12장(옛 5초 주기) 시절의 첫 정답지다.

---

## 1-a. 루프 도구 (2026-09-02 추가)

`/goal` 스킬이 절차를 고정한다. 도구는 셋이고, 앞의 둘은 **모델 없이 돈다.**

| | 무엇 | 모델 |
|---|---|---|
| `checks/answer_key.py` | 정답지(표/줄 형식)를 읽고 빈 칸을 잡아낸다 | ❌ |
| `checks/score_answers.py` | 채점 — 앱 답과 이번 근거를 **두 겹으로** 잰다 | ❌ |
| `checks/diagnose_miss.py` | 오답 자리만 **신뢰도 0.01로 재추론**해 원인을 가른다 | ✅ |

진단 딱지는 **고치는 사람으로** 가른다: 로직 / 상자밖(기계) · 임계값경계(기계,
전체 점수로 판단) · 파인튜닝(모델).

```bash
RUN=results/angle1_layout
./venv/Scripts/python.exe -m checks.score_answers $RUN --answers $RUN/angle_answer.md
./venv/Scripts/python.exe -m checks.diagnose_miss $RUN --include-evidence-gaps
```

## 2. 하지 않기로 한 것 (결정 기록)

같은 논의를 반복하지 않기 위해 근거와 함께 남긴다.

### POS기에 설치 — ❌ 파일럿 단계에서 제외
"카페 기존 PC에 설치"에서 **"기존 PC" ≠ "POS기"**로 구분한다.
1. **POS는 매장 매출의 단일 장애점.** 결제가 밀리는 사고가 한 번만 나도 그 매장을 잃는다
2. POS PC 사양이 대체로 우리 워크로드보다 낮음 (Celeron/Atom, RAM 4GB, Win10 IoT)
3. POS 벤더가 임의 설치를 금지·보증무효화하며, 원격 업데이트로 우리 프로세스를 죽일 수 있음. 결제 보안 규정 위반 소지
4. 결제 단말 + 영상 처리 결합 = 사고 시 파장 증폭
5. 랜포트 없음, OS 파편화, 파일럿 섭외 난이도 상승

**나중에 옵션 B로 볼 조건**: Intel 6세대 이상 i3급 + 여유 RAM 4GB + **벤더 서면 허용** + 프로세스 격리 + 원인 판별 로깅.
**더 나은 대안**: 매장의 POS 아닌 다른 PC(사무용·사이니지용)가 있으면 그쪽이 훨씬 안전하다.

### 카메라 2대 — ❌ 1대 확정
커버리지 부족이 확인되면 재검토. IGNORE 비율이 판단 근거. `CLAUDE.md`에 규칙으로 고정.

### 카메라 설치를 매장에 맡기기 — ❌ 우리가 고르고 우리가 단다
기종·위치·각도·높이 전부 SeatNow 팀이 정하고 직접 설치한다. 그래서 **화각이 못 보는 좌석(IGNORE)은 제품의 한계가 아니라 설치 결함**이고, 코드로 보정하지 않고 카메라를 다시 단다. 반대로 UNKNOWN(보이는데 판정 실패)은 코드로 푼다. **이 둘을 섞지 않는 것**이 사유 코드 체계의 출발점이다.

### 매장별·각도별 임계값 튜닝 — ❌ 설계에서 금지
매장에 요구할 수 있는 수작업은 **설치 당일 검수 1회가 전부**다. 기본값 하나로 굴러가야 한다. 운영 중 재캘리브레이션을 전제한 설계도 만들지 않는다.

### 바 구역을 "증거 덩어리 개수"로 세기 — ❌ 사람이 칸을 긋는다
**두 명이 붙어 앉으면 한 덩어리로 보여 1명으로 세고, 그러면 빈자리를 부풀려 "자리 있음"을 잘못 내보낸다.** 대신 설치 때 사람이 칸을 긋고, 한 사람이 칸 2개에 걸치면 UNKNOWN으로 낸다. 자동 등분은 원근 때문에 안 된다.

### 판정 임계값의 각도 불변화 (`sitting_angle` 등) — ⚠️ 보류 해제됨 (2026-09-02)
근거가 없어 미뤄뒀는데, **정답지가 생겨 조건이 충족됐다.** 실측에서 임계값
110°가 빈 구간(115.6 ↔ 135.2) **아래**에 있어 앉은 사람 하나를 서 있다고
본다는 것이 드러났다. 다만 "앉음" 표본이 1개뿐이라 **아직 옮기지 않았다** —
근거와 다음 절차는 §5-b ①.

### 카메라 즉시 구매 — ❌ 엣지 벤치 후
> **"추론 벤치로 해상도를 고른다"는 틀렸다** (2026-08-31 정정).
> `edge/bench.py`가 재는 추론은 프레임을 `imgsz`로 **줄여서** 넣는다. 그래서 카메라가 2MP든 8MP든 추론 시간이 거의 같다.
> 해상도가 실제로 잡아먹는 것은 **디코딩** — tick마다가 아니라 **24시간 내내** 돈다.
> → 카메라 선정 근거는 `edge/bench_decode.py`가 내는 **합산 표**(디코딩 + 추론)다.

구매 시 **RTSP 개방 여부 확인 필수** — 하이크비전/다후아 계열은 확정, 국내 저가 브랜드는 검증 필요.

### `imgsz` 상향 — ❌ 정확도가 오히려 떨어진다 (2026-09-02 실측)

엣지 계산량 때문에 보류하던 카드인데, 재보니 **효과 자체가 없었다.**

| 설정 | 72칸 중 옳음 | 오답 | 틱당 추론 (Ultra 5 226V) |
|---|---:|---:|---:|
| **현재 1280** | **70** | **2** | **3.3초** |
| 1536 | 68 | 3 | 3.5초 |
| 1920 | 69 | 3 | 4.0초 |
| 크롭 1280 | 69 | 2 (헛것 6) | 4.3초 |

YOLOv8n은 **640으로 학습**됐다. 1280도 이미 학습 크기의 2배고, 더 올리면
학습 때 본 적 없는 크기로 물체가 보여 못 맞춘다. **더 비싸고 더 나쁘다.**

같이 확인된 것: **크롭 재탐지가 판단 시간의 63%를 쓴다** (3.3초 중 2.1초).
프레임마다 최대 4개 × 5프레임 = 판단당 최대 20회. 그런데 **끄면 안 된다** —
끄면 70 → 67칸, 오답 2 → 5. 개수를 줄이는 것만 조절 여지가 있다.

`imgsz`와 **카메라 해상도는 별개**라는 것도 다시 확인해둔다. 추론은 프레임을
`imgsz`로 줄여 넣으므로 카메라가 2MP든 8MP든 추론 시간이 거의 같다. 카메라
값을 정하는 건 **디코딩**이고 그건 24시간 내내 돈다.

### 바닥 좌표로 판정하기 — ❌ 폐기
평면도는 **출력**이지 입력이 아니다. 판정은 화면 좌표로 한다.

---

## 3. 판정 규칙과 그 숫자들

**전부 매장 공통 기본값 하나다.** 매장별 튜닝은 설계에서 금지(§2).
숫자는 전부 **실측에서 나왔다** — angle1 6틱을 줄세워 빈 구간의 가운데를 잡는 방식.

### 시간 로직

```
빈자리 ──(점유 근거)──> 모름 ──(15초 뒤 또 근거)──> 사용중
                        └──(15초 뒤 근거 없음)──> 빈자리

사용중 ──(근거 없음 3번)──> 빈자리     ← 기다리는 동안은 "사용중"을 유지한다
```

- **판단 주기 15초 고정.** 5초 2차 판단은 제거했다(2026-09-02) — 엣지 계산량이 예측 가능해야 한다. 같은 영상에서 12틱 → 6틱.
- 한 판단 = **연속 5프레임 다수결**(`--median-frames 2`)
- 확정 횟수: 점유 **2번** / 빈자리 **3번** (`occupy_confirmations` / `empty_confirmations`)
- **모름은 판단을 지우지 않고 멈추기만 한다.** 사람이 잠깐 가려도 쌓아둔 근거가 유지된다
- 비대칭인 이유: 빈자리를 성급히 사용중이라 하면 손님이 불편할 뿐이지만, 사용중을 성급히 빈자리라 하면 **손님이 남의 자리로 간다**

### 물건 → 자리 배정 (`engine/seatnow_core.py`)

| 규칙 | 값 | 근거 |
|---|---|---|
| 사람이 그린 좌석은 **그린 상자를 그대로** 쓴다 | — | `table_surface_box` 추정은 탐지된 dining table 상자용이다. 그린 상자에 씌우면 키 큰 테이블의 띠가 위로 뻗어 뒤 테이블을 덮는다 |
| 한 물건은 **가장 많이 덮는 자리 하나**에만 | — | 점수가 50% 덮으면 포화돼서 동점이 되고, 승자가 자리 목록 순서로 정해졌다 |
| `MAXIMUM_OBJECT_SEAT_AREA_FRACTION` | **0.50** | 자리를 절반 넘게 채우는 "물건"은 가구를 잘못 본 것. 진짜 짐 30% 이하 ↔ 유령 80% 이상.<br>**의자에도 같은 값을 쓴다**(2026-09-02, 전엔 0.9): 진짜 의자 짐 12건 전부 44% 이하 ↔ 벤치 63% |
| `MINIMUM_OBJECT_SEAT_OVERLAP` | **0.20** | 자리에 이만큼도 안 걸친 물건은 그 자리 것이 아니다. 진짜 26% 이상 ↔ 틀린 것 13% 이하 |
| `MINIMUM_SEAT_OCCLUSION` | **0.50** | 사람이 이만큼 덮으면 "안 보인다"로 본다. 가림 55% 이상 ↔ 스쳐 지나감 38% 이하 |

### 자리 종류별 규칙

- **테이블**: 짐 **또는** 앉은 사람 **또는** 짐/사람이 얹힌 의자 → 사용중
- **바(`counted_zone`)**: **사람이 있어야만** 사용중. 짐만 있으면 빈자리(`belongings_only`).
  한 사람이 바에 노트북·컵·가방을 펼치면 세 칸이 다 사용중이 되어 "바 만석"이 뜨는 쪽이 더 나쁘다. **2026-09-02 재확인, 유지**
- **의자↔테이블 연결은 사람이 그린 묶음에서 온다** — 상자 모양과 무관하다 (`seatnow_core.py:1629`)

### 사용중 근거 글자

`X T5 (t)` 처럼 사진에 붙는다. `s` 사람이 앉음 / `t` 책상에 짐 / `c` 의자에 짐.
로그의 물건마다 `box`와 `share`(이 물건의 몇 %가 이 자리 안에 있나)가 남는다. **배정을 결정한 그 순간의 값**이다.

### 검수 화면과 손님 앱은 일부러 다르다

이번 판단에서 아무 근거도 못 봤는데 사용중을 유지하는 자리는, **검수 폴더에서만 회색(모름)** 으로 보여준다. 손님 앱에는 사용중으로 나간다 — "곧 빌 것 같다"를 앱에서 지우면 더 혼란스럽다.
앱 계약(`seat_report`)은 건드리지 않았다. `checks/make_review.py`의 `review_state()`만 다르게 본다.

---

## 4. 사람이 그린 레이아웃 (`layouts/cafe_angle1.json`)

**총 12자리 = 테이블 6(T1~T6) + 바 6칸(BAR7-1~6).**
테이블은 의자가 몇 개든 `capacity: 1`이다. 앱 문구는 "3자리 남음"이 아니라 **"빈 테이블 3개"**가 정직하다 (결정 대기).

### 상자를 다시 그린 기록

세 자리 모두 **같은 원인**이었다 — 사각형 하나로는 원근 속 테이블을 감쌀 수 없어서, 상자가 테이블 밖의 벤치·바닥까지 덮고 그 위의 물건이 그 자리 것으로 들어왔다.

| 자리 | 전 | 후 | 무엇이 잘못 들어왔나 |
|---|---|---|---|
| T3 | `877,322,1012,463` | `880,368,1000,418` | 뒤쪽 벤치의 회색 쿠션이 `suitcase`로 |
| T3 의자2 | `939,309,1067,391` | **삭제** | 의자가 아니라 창가 카운터·창문이었다. 그 안의 벤치가 `suitcase`로 잡혀 빈 자리를 사용중으로 만들었고, 벤치에 맞춰 다시 그려보니 **거기 앉은 사람이 있어도 정답은 T3 빈자리**였다 — T3의 자리가 아니다 |
| T4 | `1474,439,1660,549` | `1462,439,1668,568` | 아래변이 짧아 T4/T5 사이에 어느 자리도 아닌 띠가 생겼다 |
| T5 | `1373,550,1641,850` | `1355,583,1648,763` | 위로는 T4 상판, 아래로는 바닥 |

T4의 위쪽(439)은 일부러 안 건드렸다 — **BAR7 구역이 이미 T4 위로 크게 겹쳐 있어서** 위로 넓히면 바 위의 물건이 T4로 딸려온다.

**T1·T2·T4·T6은 아직 점검하지 않았다** (§1의 후보 4번).

좌석 상자는 이제 **사람이 그린 자리에서 출발한다.** 예전엔 탐지된 가구 쪽으로
끌려가 첫 판단부터 밀려나 있었다 (§1의 고친 것 ①).

### 평면도 편집기

```bash
./venv/Scripts/python.exe -m install.floorplan_editor --layout layouts/cafe_angle1.json \
  --log results/angle1_layout/log.jsonl
```

확대·이동, 벽/입구/카운터/소파/창문 그리기, 회전·크기, 2인 의자, 의자 숨기기까지 된다.
**바닥 네 점이 잘못 찍혀 있어 처음 위치는 많이 틀리다** — 손으로 옮겨야 한다. 실제로 그리는 작업은 보류 중.

---

## 5. 남은 일

### 트랙 A — 소프트웨어 (지금 할 수 있는 것)

| | 내용 | 상태 |
|---|---|---|
| §1 | 판정 개선 루프 | ⏳ **진행 중** |
| T15 | 설치 각도 선정 — angle1~4를 같은 방식으로 비교 | ⏸ angle2~4 결과는 지웠다. 영상은 남아 있어 언제든 다시 돌린다 |
| T16 | 좌석별 라벨링 (`checks/make_labels.py`) | ⬜ **각도 확정 후.** 이 플랜에서 가장 비싼 사람 노동 |
| T3 | 평가셋 — 도구 완료, 라벨 대기 | ⬜ T16에 물려 있음 |
| T11 | 파인튜닝 | ⬜ 조건부 (아래) |

**트랙 A의 순서는 지켜야 한다.** 라벨링을 먼저 하면 설치 각도가 안 정해진 상태에서 라벨을 만들게 되고, 각도가 바뀌면 그 노동이 통째로 버려진다.

### 트랙 B — 엣지 박스 ⬅️ **내일 여기서 시작**

박스가 왔다: **OptiPlex 7040 Micro / i3-6100T / RAM 4GB / Linux.**
절차는 **`docs/edge-first-run.md`** (긴 절차인 `docs/edge-setup.md`보다 이게 먼저다).

| | 내용 |
|---|---|
| **T6** | ⬅️ **내일.** `check_edge --sample` → 실행 → 채점. 합격선 **틱 7.5초**.<br>지금 설정 그대로면 **13~20초 예상**이라 카드를 써야 할 것이다:<br>① `--median-frames 1`(5→3프레임, **59% · 정확도 손해 0**) ② OpenVINO ③ RAM 8GB ④ `--max-crops 2`(1칸 대가)<br>⚠️ `check_edge`의 "코어 4개 합격"은 **논리 코어**다. 실제 연산 코어는 2개 |
| T7 | 파라미터 스윕 (`edge/bench_sweep.py`) — T16 라벨 + T6 예산 필요.<br>**정답지 72칸으로 이미 일부를 손으로 재뒀다** (§5-a 표) |
| 카메라 확정 | T6의 **합산 표**(디코딩 + 추론)로 해상도를 고른다 |
| T8 | RTSP 스트림 리더 ⚠️ 아키텍처 변경 |
| T10 | 배포 프로파일 — **`--no-video`가 기본값.** 주석 MP4를 디스크에 쓰면 "영상 미저장" PIPA 예외가 깨진다 |

### 5-a. 설정별 비용·정확도 실측표 (2026-09-02, 노트북 기준)

엣지에서 시간이 모자랄 때 **어느 손잡이를 돌릴지 미리 재둔 것**이다.
정확도는 전부 같은 정답지 72칸 기준.

| 버스트 | 크롭 | ms/tick | 옳음/72 | 오답 | 지금 대비 |
|---:|---:|---:|---:|---:|---:|
| 5프레임 | 4 | 3276 | **70** | 2 | 100% (기본값) |
| **3프레임** | **4** | **1920** | **70** | **2** | **59%** ⬅️ 공짜 |
| 5프레임 | 2 | 2076 | 69 | 3 | 63% |
| 3프레임 | 2 | 1265 | 69 | 3 | 39% |
| 3프레임 | 1 | 977 | 69 | 3 | 30% |
| 1프레임 | 4 | 750 | **56** | 4 | 23% |
| 크롭 끔 | — | 1217 | **67** | 5 | 37% |

- **버스트 5→3프레임은 공짜다.** 정확도가 그대로인데 41%가 줄어든다
- **1프레임은 절대 안 된다** (70 → 56). 다수결이 실제로 일을 하고 있다
- **크롭을 아예 끄는 것도 안 된다** (70 → 67). 개수를 줄이는 것만 여지가 있다
- ⚠️ **6틱(78초)에서 나온 표다.** 다수결은 탐지가 순간적으로 흔들리는 것을
  막는 장치인데 78초에는 그런 흔들림이 몇 번 없다. **긴 영상이 생기면
  3프레임이 여전히 안전한지 다시 확인해야 한다.** 그래서 기본값은 5프레임으로
  두고 엣지에서 켜는 옵션으로 남겼다

---

### 5-b. 코드로 더 갈 수 있는 방향

**전부 이번 루프에서 숫자로 드러난 것들이다.** 추측으로 적은 항목은 없다.
위쪽이 값이 크고 근거가 단단한 순서다.

#### ① `sitting_angle` 재검토 — UNKNOWN을 줄이는 가장 값싼 길

§2에서 "어느 임계값이 병목인지는 평가셋이 나온 뒤에"로 미뤄둔 항목인데,
**정답지가 생겨서 조건이 충족됐다.**

가림 모름 9칸을 만든 사람들의 자세 판정을 다 뽑아봤더니, **전부
`standing`(서 있음)** 이었다. 그런데 그중 하나는 정답이 "앉아 있다"이다.

| 시각 | 자리 | 정답 | 자세 판정을 가른 각도 | |
|---|---|---|---:|---|
| **30초** | **T3** | **X(s) 앉음** | **115.6°** | ❌ 임계값 110°를 **5.6도** 넘겨 '섬'이 됐다 |
| 45초 | T3 | O 빔 | 135.2° | ✅ |
| 0초 | BAR7-4 | O 빔 | 157.4° | ✅ |
| 45초 | BAR7-3 | O 빔 | 160.5° | ✅ |
| 75초 | BAR7-4 | ? | 167.4° | ✅ |
| 60초 | T3 | O 빔 | 172.8° | ✅ |
| 15초 | T3 | O 빔 | 176.2° | ✅ |

**115.6 ↔ 135.2 사이가 비어 있다.** 지금 값 110은 그 빈 구간 **아래**에 있어서,
앉은 사람 하나를 서 있다고 본다. `plan.md` §3의 방식(실측을 줄세워 빈 구간의
가운데를 잡기)대로면 **125 근처**가 후보다.

⚠️ **데이터 하나로 임계값을 옮기지 말 것.** "앉음" 쪽 표본이 1개뿐이다.
올리면 서 있는 사람을 앉았다고 볼 위험이 같이 커진다. **72칸 전체로 검증하고,
나빠지면 되돌린다** (`/goal` 스킬의 규칙).

#### ② 버려진 탐지를 로그에 남기기 — 파인튜닝 데이터 수집이 자동화된다

지금 엔진은 제외 클래스(`chair`, `dining table` …)를 **조용히 버린다.**
그래서 로그만 보면 "모델이 아무것도 못 봤다"로 보이는데, 실제로는 **매번
정확한 자리에서 봤고 이름만 틀린** 경우가 있었다 (T5의 노트북을 `chair` 0.20).

이번엔 `checks/diagnose_miss.py`가 사진을 다시 추론해서 알아냈지만, 그건
**정답지가 있는 6장에만 되는 일**이다.

→ **`raw_detections`에 "좌석 위에 있었지만 제외 클래스라 버린 것"을 남기면**,
운영 중 영상에서 파인튜닝 후보가 저절로 쌓인다. T11의 데이터 수집 비용을
직접 깎는다. 판정 동작은 하나도 안 바뀐다 (로그만 늘어난다).

#### ③ 좌석을 사각형 하나로 표현하는 것의 한계

`plan.md` §4가 이미 적은 문제인데, 이번에 **숫자로 확인됐다.**

- 30초 T5: 상판 안쪽 끝에 **세워둔 노트북이 상자에 5%만 걸친다.** 원근 때문에
  물체가 상자 위로 솟기 때문이다
- 같은 물체를 **옆 테이블 T4의 상자가 46% 덮는다.** T4와 T5는 깊이가 다른데
  화면에서는 겹친다
- T5 윗변을 올려봤더니(583→550) 이번엔 **크롭 재탐지의 크롭이 커져 해상도가
  떨어지면서** 75초의 `laptop`을 놓쳤다 (70 → 69)

→ 축정렬 사각형 하나로는 원근 속 상판을 담을 수 없다. 후보:
**상판을 네 점 다각형으로 받기** (`install/floorplan.py`에 이미 호모그래피가
있다), 또는 **좌석마다 "물건이 솟는 높이"를 두기**.
⚠️ 스키마 변경이라 `install/calibrate.py`·편집기·`seatnow_layout.py`가 같이
움직인다. **엣지 숫자가 나온 뒤에 착수할 것.**

#### ④ 크롭 재탐지가 좌석 크기에 따라 성능이 달라진다

크롭 영역을 테이블 크기에 비례해 잡고(`pad = 0.65×너비, 0.80×높이`) 그것을
**960으로 줄여서** 추론한다. 그래서 **큰 테이블일수록 유효 해상도가 낮아진다.**
T5 상자를 33픽셀 넓힌 것만으로 `laptop`을 놓친 게 그 증거다.

→ 크롭을 **고정 픽셀 크기로 자르거나**(타일링), **크롭 실제 크기에 맞춰
`crop_imgsz`를 정하면** 좌석 크기와 무관해진다. 이건 정확도와 엣지 비용에
동시에 걸리는 항목이라 **T7 파라미터 스윕과 같이 봐야 한다.**

#### ⑤ UNKNOWN 사유 분포를 채점표에 자동 집계

`CLAUDE.md`가 "사유별 분포가 곧 다음 개선 백로그다"라고 정했는데, 지금은
사유가 채점표에 흩어져 있어 사람이 세야 한다. `engine/seatnow_report.py`에
`REASON_GROUPS`(install / geometry / model / time / settled)가 이미 있으니,
**`checks/score_answers.py`가 그 분류로 한 줄 표를 뽑으면** 다음에 뭘 고칠지가
매번 자동으로 나온다. 작고 값이 확실한 작업이다.

#### ⑥ 좌석 상자 드리프트의 장기 거동

가구가 움직인 만큼만 따라가게 고쳤지만, **75초에 T1·T5·T6이 최대 50픽셀**
움직인다. 카메라 미세 흔들림을 따라가는 것이라 지금은 점수에 도움이 되는데,
**몇 시간 단위로 무한히 흘러가는지는 78초 영상으로 확인할 수 없다.**

→ 후보: **그린 상자에서 일정 비율 이상 못 벗어나게 상한을 두고, 상한에 닿으면
로그에 경고를 남긴다.** 상한에 자주 닿으면 그건 카메라가 흔들린다는 신호이고,
코드가 아니라 **설치를 다시 볼 일**이다 (`CLAUDE.md`).
**긴 영상이 생기기 전에는 숫자를 정할 수 없다.**

---

### T11 파인튜닝 — 선행 조건 3개, 하나라도 빠지면 시작 금지

1. 판정 기준선 확보 (§1)
2. T16 라벨 (측정할 자)
3. T6 (**목표 `imgsz` 확정** — 640용과 1280용은 학습 설정이 다르다).
   → 실측으로 **1280 유지**가 정해졌다 (§2). 남은 것은 엣지에서 이 설정이
   7.5초 안에 드는지 확인하는 것뿐이다

**T7에서 정확도가 충분하면 파인튜닝은 불필요하다.**

- detect 모델만 대상. **pose 모델은 사실상 불가** — keypoint 17개 라벨링은 비용 차원이 다르다. COCO 유지
- 클래스 축소 COCO 80 → 12개 내외. ⚠️ 축소 시 `EXCLUDED_OBJECT_CLASSES`(`engine/seatnow_core.py`)와 클래스명 매핑을 **함께** 고칠 것. **이름이 바뀌면 판정이 조용히 오작동한다**
- 데이터 300~600장, Google Colab (T4). 예상 2~3주

---

## 6. 보유 영상

`sample_raw/` — 저장소에 없음(`.gitignore`), 총 1.6GB

| 파일 | 해상도 | 길이 | fps | 용도 |
|---|---|---:|---:|---|
| `cafe_sample_angle1.mov` | 1920×1080 | 78초 | 30 | **지금 쓰는 것** |
| `cafe_sample_angle2~4.mov` | 1920×1080 | 63~78초 | 30 | 각도 선정 (T15) |
| `cafe_sample_그냥착석 / 멀리앉기 / 의자옮김.mov` | 1920×1080 | 9~13초 | 30 | 데모 전용 |
| `moonq_sample_longform.mp4` | **1280×720** | 631초 | **120** | 성격 확인 필요 |

**여기서 평가셋의 전제가 틀렸다는 게 드러난다.**

1. **시나리오 3개(9~13초)는 채점에 쓸 수 없다.** 판단 주기 15초보다 짧다. 눈으로 보는 데모용이다 (`--sample-seconds 5`로 줄여 실행)
2. **angle1~4는 합쳐서 4.8분.** 파라미터 A/B 차이를 가릴 통계력이 없다. **설치 각도 선정용**이다
3. 시나리오 3개는 서로 같은 각도다 — 레이아웃 JSON 하나로 커버된다
4. `moonq_sample_longform`은 촬영 장비가 다르다. **같은 카페의 고정 촬영인지 확인 필요** — 맞다면 유일한 실질 평가셋 후보(10.5분). 아니면 각도 확정 후 재촬영

**상태 전이는 어느 영상으로도 거의 못 잡는다.** 이탈 확정에 45초가 필요하고, 78초 영상에는 완전한 "앉았다 일어남" 사이클이 한 번 겨우 들어간다.

---

## 7. 끝난 것 (한 줄 기록)

되짚을 일이 있으면 `git log`를 본다. 여기엔 **지금 코드를 이해하는 데 필요한 것만** 남긴다.

| | 내용 |
|---|---|
| T1 | 의자 연결 회귀 복구 — **가림이 상시적이라 의자 연결·짐 증거가 부가 기능이 아니라 주 경로다** |
| T2 | 진단 로깅 (`--log-detections`) — "모델이 못 봤나" vs "코드가 버렸나"를 로그로 가른다 |
| T4·T5 | OpenVINO 익스포트, RTSP 재송출 하네스 |
| T9·T17 | 하드웨어 디코딩(`--hwaccel`), 디코딩 벤치 |
| T12 | 문서 |
| T13 | `seat_report` 출력 계약 + 바 좌석 칸 + UNKNOWN 사유 코드 |
| T14 | 실영상 첫 실행 + 검출 검사 하네스 (`checks/inspect_run.py`, `checks/judge_frames.py`) |
| 2D 평면도 | 스키마 v3, 호모그래피 투영, 브라우저 편집기 (1~3단계) |
| 저장소 재배치 | `engine` / `install` / `checks` / `edge` / `results` |
| 검수 폴더 | `checks/make_review.py` — 영상 재실행 없이 `log.jsonl` + `clean/` 으로 다시 그린다 |

### 2026-09-02 정답지 루프에서 고친 것

| 커밋 | 내용 |
|---|---|
| `013204c` | 정답지 도구 셋 + `/goal` 스킬 |
| `c1983a2` | 좌석 상자는 **위치가 아니라 이동량**을 따라간다 (67 → 69칸) |
| `3d496f8` | 의자 위 짐도 절반 넘으면 가구 + T3의 없는 의자 삭제 (69 → 70칸) |
| `88f082f` | 진단이 "못 본다"와 "이름을 틀렸다"를 가른다 |

### 2026-09-02 오전에 고친 것 (사람이 사진 보던 시절)

| 커밋 | 내용 |
|---|---|
| `d216f2d` | 사용중 근거 글자 `s`/`t`/`c` |
| `a317f19` | 판단 주기 15초 고정, 확정 전 중간 상태를 모름으로 |
| `3ee8e72` | 사람이 그린 좌석에 "상판 띠" 추정을 씌우지 않는다 |
| `b213e04` `fd327d5` `f186fa9` | T5·T4·T3 상자 다시 그리기 |
| `8ea9f83` | 한 물건은 가장 많이 덮는 자리 하나에만 |
| `537cb43` | 자리를 절반 넘게 채우는 "물건"은 가구 |
| `50da709` | 자리에 20%도 안 걸친 물건은 그 자리 것이 아니다 |
| `d44008a` | 사람이 가린 자리는 빈자리가 아니라 모름 |

### 윈도우에서 걸린 것 (다시 밟지 말 것)

- **OpenCV는 한글이 든 경로에 `imwrite`가 조용히 실패한다.** 파일이 안 생기는데 오류도 안 난다 → `cv2.imencode` + `Path.write_bytes`
- 윈도우 파일 이름에 `?`를 못 쓴다 → 전각 `？`
- 콘솔이 cp949라 `—`(em dash) 출력이 터진다
