# SeatNow — CCTV 좌석 점유 감지 시스템 전체 정리

> SSATIS 팀 (4인) / YOLOv8 기반 카페 좌석 점유 감지
> 이 문서는 지금까지의 논의 + 구현 내용 + 앞으로 할 일을 한 곳에 정리한 것
> 최종 갱신: 2026-07-11 (영상 엔진 + 시간 안정화 + 점유판정 로직 개선 반영)

---

## 1. 프로젝트 개요

카페 CCTV 영상을 분석해서 좌석(테이블) 점유 여부를 실시간으로 판단하고, 손님용 앱에 "몇 자리 남음"을 보여주는 시스템.

- **참고 논문**: Seatify (IEEE ICARC 2026, DOI: 10.1109/ICARC68737.2026.11453649) — 스리랑카 대학 졸업작품, 상업 제품 아님 → 방법론 자유롭게 구현 가능
- **개발 환경**: MacBook Pro 2018 (Intel i7, 16GB RAM, GPU 없음), Python 3.9.6, 가상환경(`venv`) 사용
- 초기 numpy 2.x ↔ PyTorch 호환성 충돌 → `numpy<2`로 다운그레이드해서 해결

---

## 2. 점유 판정 로직 (핵심 — 최종 확정본)

### 판정 조건 (OR)
테이블 ROI 내부에 아래 중 **하나라도** 있으면 점유:
1. **비-사람 물체**가 있음 (컵, 노트북, 가방 등)
2. **앉아있는 사람**이 감지됨 (포즈 추정으로 판별)
3. **테이블에 연결된 의자가 점유됨** (앉은 사람 또는 의자 위에 놓인 짐)

→ ROI에 **서있는 사람**이 겹쳐도 점유로 안 잡힘 (의도된 설계)

### 오탐 방지 장치 (2026-07-11 확정)
- **들고 있는 짐 제외**: 앉지 않은 사람의 박스와 60% 이상 겹치는 물체는 점유 근거에서 제외 — 짐을 들고 지나가는 손님, 좌석을 가린 사람 몸 위의 환각 객체(banana 오탐 등) 차단
- **의자 강한 링크(0.75) / 약한 링크(0.35) 분리**: 의자→테이블 점유 전파는 테이블에 딱 붙은 의자(강한 링크)만 허용. 옆 테이블 손님을 흡수하는 문제 방지. 약한 링크는 구조 파악(테이블 구제·표시)에만 사용
- **연쇄 강등 방지**: "이전 프레임 standing이면 약한 seated 강등" 규칙이 자기 자신이 만든 standing에는 재적용되지 않도록 수정 (한 번 걸으면 영원히 standing으로 고정되던 버그)

### 테이블 후보 선별 (`select_table_candidates`)
YOLO의 dining table 탐지를 그대로 쓰지 않고 아래 규칙으로 선별:
- 화면 면적 6% 이하(soft cap) + conf ≥ 0.20 → 통과
- 6% 초과 대형 박스는 conf ≥ 0.30 이거나 **의자 2개 이상이 구조적으로 붙어 있으면** 통과 (전경 대형 테이블 구제 — 기존 6% 일괄 컷이 conf 0.80짜리 진짜 테이블까지 버리던 문제 해결)
- conf 0.12~0.20 약한 후보도 의자 2개 이상 지지 시 구제 (뒷줄 테이블 미탐 해결)
- 화면 40% 초과(hard cap)는 무조건 거부
- 두 테이블을 삼킨 병합 박스 거부: 승인된 테이블 2개 이상을 포함하거나, 자기보다 작은 승인 테이블과 30% 이상 겹치는 대형 박스는 제거 (작은 테이블의 물체 증거를 빼앗는 문제 방지)

### 물체 판정에서 제외하는 클래스
가구류는 "물체 존재"로 안 침:
`chair, couch, dining table, bench, potted plant, tv, refrigerator, cat, dog`

### 포즈 판정 (앉음/서있음)
YOLOv8-pose로 17개 keypoint 추출 후, 아래 2개 테스트를 **OR**로 조합:
- **Test 1 (Hip-Knee-Ankle 각도)**: 엉덩이-무릎-발목 각도 < 110° → 앉음
- **Test 2 (Torso 각도)**: 어깨-엉덩이-무릎 각도 < 110° → 앉음

Seatify 논문엔 3번째 테스트(Hip-Knee 수직 거리)도 있었지만, **우리는 제외**함 — 서서 몸을 굽히는 동작에서 오탐(false positive)이 발생했기 때문.

### ROI 방식: 동적 ROI
- 고정 ROI 대신, 매 프레임 테이블 bounding box 탐지 → 테이블 중심 기준으로 반경 R의 ROI를 매번 재계산
- 이유: 손님이 테이블/의자를 자주 옮기기 때문에 고정 좌표로는 안 됨

### 레이아웃 모드 (`--layout`)
캘리브레이션된 존(`calibrate.py`로 등록한 `layouts/*.json`)이 좌석 기준. 좌석 수·ID·의자 연결이 고정되어 앱 표시가 안정적이며, 존 밖 탐지는 완전 무시되고 inferred-seat도 비활성. 사람·물체 증거만 자동으로 채운다. 경량 모델(yolov8n)과 병용을 전제로 한 운영 모드.

**존 실시간 트래킹(기본 켜짐, `--no-layout-track`으로 끔):** 가구가 움직이면 존이 자기와 IoU ≥ 0.30으로 겹치는 탐지 박스를 EMA(α=0.35)로 따라간다. 탐지가 없는 존(모델이 못 보는 가구의 수동 ROI)은 마지막 위치에 고정. 한 탐지가 두 존과 비슷하게 겹치면 아무 존도 움직이지 않는다(오추적 방지). 화면 경계에 그린 수동 존은 border-crop 무시 규칙에서 제외된다.

---

## 3. 구현된 코드 (영상 처리까지 완료)

| 파일 | 역할 |
|------|------|
| `seatnow_core.py` | **본체** (~2,100줄): 추론·판정·의자/물체/사람 연결·`TableTracker`(추적+디바운싱)·장면전환 감지·FFmpeg 영상 입출력·렌더링·JSONL 로깅 |
| `seatnow.py` | CLI 진입점 — 이미지 1장 & mp4 영상 모두 처리, 주석 영상 + JSONL 로그 출력 |
| `verify_seatnow.py` | 실행 결과 JSONL을 수동 라벨 정답(`tests/fixtures/test_video_expectations.json`)과 대조 검증 |
| `tests/test_seatnow_core.py` | 유닛 테스트 54개 — 판정 로직은 전부 모델 없이 순수 함수로 테스트 (실제 실패 사례 좌표 기반 회귀 테스트 포함) |
| `occupancy_mvp.py`, `pose_judge.py` | 초기 PoC (참고용으로만 유지) |
| `seatnow_layout.py` | 수동 좌석 레이아웃 로드/검증/스케일 |
| `calibrate.py` | 클릭 캘리브레이션 도구 (테이블·의자 등록 → `layouts/*.json`) |

- 사용 모델: pretrained YOLOv8x, YOLOv8x-pose
- OpenCV 휠에 비디오 백엔드가 없는 환경이라 **영상 디코딩/인코딩은 ffmpeg 파이프로 직접 처리** (`FFmpegSampleReader`/`FFmpegVideoWriter`)
- 시간 안정화: 점유 확정 2프레임 / 빈자리 확정 3프레임(비대칭 디바운싱), 트랙 TTL, 장면 전환 시 리셋
- 테스트 자산: `sample_raw/`에 시나리오별 촬영 영상 14개 (착석, 짐 점유, 가림, 짐 옮김, 의자 위 가방 등), 결과는 `sample_results/v5_*.{mp4,jsonl}`
- 알려진 약점:
  - 비스듬한 각도에서 나란한 긴 테이블 2개가 한 박스로 병합됨 (fine-tuning으로 개선 필요)
  - 팬(이동) 촬영 영상은 프레임별 정확 집계가 어려움 — fixture 검증 5/20 (고정 CCTV가 제품 타깃이므로 허용, 전이 횟수 검증은 통과)

---

## 4. 모델 전략 (개발 → 배포)

```
YOLOv8n (nano) pretrained
        ↓ fine-tuning
카페 데이터로 학습된 fp32 모델 (원본, 계속 재학습 가능하도록 보존)
        ↓ 배포 시점에만
INT8 양자화 → 엣지 디바이스에 배포되는 스냅샷
```

- 원본(fp32)과 배포용 양자화본은 분리 관리 — 양자화는 "배포용 스냅샷 뜨기"이고 원본은 계속 학습 가능
- **Fine-tuning 데이터**: Roboflow Universe 공개 데이터셋 우선 활용
  - Seat Analysis
  - Deep Vachhani emptyoccupied
  - Person-on-Chair
  - chair detection 2
  - chair+with object
  - 이후 파일럿 카페에서 익명화된 실제 프레임 추가
- 학습 환경: Google Colab (T4 GPU)

---

## 5. 개인정보보호법(PIPA) 대응

- **실시간 추론 (영상 미저장)**: 2024년 개정 예외 조항(개인정보보호법 제25조1항6호, 시행령 제22조1항) — 통계처리 목적으로 영상을 저장하지 않으면 예외 적용됨. 이 경우 얼굴 모자이크 불필요.
- **학습용 실제 카페 영상 사용 시**: 얼굴 블러링/익명화 필수 + 카페 사장님과의 계약에 "AI 학습 목적 데이터 활용 동의" 조항 포함 + 원본은 일정 기간 후 파기
- 포즈 추정은 관절 좌표만 쓰므로 얼굴 블러해도 알고리즘엔 영향 없음
- **주의**: 실제 실행 전에는 변호사 확인 권장 (Claude가 법률 자문을 대체하지 않음)

### 운영 3원칙
1. 영상 자체는 저장하지 않음 (상태값만 전송)
2. 개인 식별은 하지 않음 (점유/비점유만 판단)
3. 카페 사장님이 운영 주체

---

## 6. 엣지 디바이스 / 배포 구조

### 배포 단계
- **MVP**: 카페 기존 PC에 소프트웨어 설치 (하드웨어 비용 0, 경량 모델 필요)
- **확장 시**: 대여형 미니PC 또는 라즈베리파이/Jetson Nano → RTSP로 IP카메라 스트림 수신

### CCTV 호환성
- ✅ 가능: IP카메라(RTSP/ONVIF), NVR+IP카메라, RTSP 지원 클라우드형
- ⚠️ 조건부: 일부 클라우드형(RTSP 개방 모델만), 아날로그+DVR(변환 시)
- ❌ 불가: 순수 아날로그 구형, 완전 폐쇄형 클라우드
- 국내 주요 브랜드(한화비전, 아이디스, 하이크비전, 다후아) 대부분 RTSP 지원 → 하나의 방식으로 커버 가능

### 데이터 흐름
```
[카페 안]                    [클라우드]        [손님]
CCTV → NVR → 엣지 디바이스  →  Supabase   →  SeatNow 앱
       (실시간 분석, 영상은 여기서 폐기)   (상태값만)   (빈자리 확인)
```

---

## 7. 기술 스택

| 역할 | 선택 |
|------|------|
| CV 추론 | Python + Ultralytics YOLOv8 (기본 yolov8n, 정확도 우선 시 yolov8x) + OpenCV |
| 모델 서빙 | FastAPI (또는 Next.js API Route) |
| 학습 환경 | Google Colab (T4 GPU) |
| 실시간 DB | Supabase Realtime |
| 프레임 큐 | Redis (확장 시) |
| 엣지 | 라즈베리파이 / Jetson Nano |
| 카메라 연결 | RTSP / ONVIF |

---

## 8. 전체 로드맵

**현재 위치**: 영상 처리 + 시간 안정화 + 판정 로직 검증까지 완료 — **"작동하는 데모" 단계 도달, 파일럿 카페 섭외 가능**

```
✅ 1. 물체 기반 점유 판정 — 완료
✅ 2. 포즈 기반 앉음/서있음 판정 — 완료
✅ 3. 둘 통합 — 완료

✅ 4. [Stage 1] 영상 처리 엔진 — 완료
    mp4를 프레임 단위(1초당 1장)로 처리, 테이블별 점유 판정 → 주석 영상 + JSONL 로그

✅ 5. [Stage 2] 시간 안정화 로직 — 완료
    비대칭 디바운싱(점유 2 / 빈자리 3프레임), 테이블 추적(TableTracker), 장면 전환 리셋
    + 판정 로직 개선(2026-07-11): 테이블 선별 규칙, 의자 연결 점유, 소지품/환각 오탐 차단
    (baseline 비교 방식의 고정 물체 오탐 제외는 미구현 — 필요 시 파일럿에서 판단)

   ── ✅ 여기까지 완성 → 지금은 파일럿 카페 섭외 단계 ──

   6. [Stage 3] Fine-tuning
    파일럿 카페 실 데이터 + Roboflow로 YOLOv8n 학습, 목표 정확도 92%

   7. [Stage 4] 좌석 캘리브레이션 도구
    CCTV 화면에서 테이블/좌석 위치 등록하는 어드민 UI

   8. [Stage 5] 백엔드 연동
    상태값만 Supabase로 전송(영상 미저장), 실시간 반영

   9. [Stage 6] 손님용 앱 화면
    "몇 자리 남음" 표시 + 시팅맵 시각화

   10. [Stage 7] 사장님 배포 패키지
    카페 PC/소형기기 설치 패키징, RTSP 설정 가이드, 개인정보 처리방침/안내판 문구

   11. (더 나중) 수요 예측 분석 대시보드 (XGBoost 등)
```

### 우선순위 근거
1~2단계(영상처리+안정화)가 지금 가장 가치 있음 — "작동하는 데모"가 있어야 파일럿 카페를 섭외할 수 있고, fine-tuning(3단계)은 그 파일럿 카페의 실제 데이터가 있어야 제대로 되기 때문에 순서상 뒤로 미뤄짐.

```
[1~2단계: 영상처리+안정화]
        ↓
[파일럿 카페 섭외]
        ↓
[3단계: 그 카페 데이터로 fine-tuning]
        ↓
[4~5단계: 캘리브레이션+백엔드]
        ↓
[6~7단계: 앱+배포]
```

---

## 9. 지금 당장 다음 작업

**파일럿 카페 섭외 + Stage 3 (fine-tuning) 준비**
- 데모 자료: `sample_results/v5_*.mp4` (시나리오별 주석 영상) 활용
- 파일럿 카페 확보 후 실제 CCTV 프레임으로 YOLOv8n fine-tuning (병합 테이블·저신뢰 테이블 문제의 근본 해결책)
- 실행 예시: `./venv/bin/python seatnow.py sample_raw/영상.mp4 --debug`
- 검증: `./venv/bin/python -m unittest discover tests` (54개), `./venv/bin/python verify_seatnow.py <결과.jsonl>`

---

## 10. 참고 문서 (이미 만들어둔 것)

과거 대화에서 아래 두 개의 인수인계 문서를 이미 만들어뒀음 (Codex용):
- **SEATNOW_HANDOFF.md** — 기술 인수인계 (알고리즘, 파일 구조, 환경, Roboflow 데이터셋 목록, Seatify 논문 요약)
- **SEATNOW_STRATEGY.md** — 전략/법무 (개인정보보호 대응, 학습 전략, 양자화 라이프사이클, 엣지 디바이스, 7단계 로드맵)

이 문서(`SEATNOW_전체정리.md`)는 그 둘을 하나로 합치고 최신 상태로 갱신한 종합본임.
