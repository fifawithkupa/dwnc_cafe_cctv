# plan.md — 남은 할 일

2026-09-11 에 내용을 비우고 다시 썼다. 이전 판(설계 결정 기록, T1~T19 등)은
git 이력에 있다: `git show a6320ef:문서/plan.md`.
판정 로직 배경은 `문서/SEATNOW_전체정리.md`, 기록 설계는 `문서/판정개선_데이터설계.md`.

## 지금 바로 (사람이 해야 하는 것)

| # | 할 일 | 왜 | 어떻게 |
|---|---|---|---|
| 1 | **Supabase 표 정리** — 대시보드 SQL Editor 에서 `deploy/supabase/schema.sql` 과 `deploy/supabase/telemetry.sql` 을 각각 한 번씩 실행 | 코드는 지웠지만 서버에 남은 사진 표(`cafe_seat_sheets`)·사진 저장소(`seat-sheets`)·옛 기록 표(`seatnow_runs`, `seatnow_scenery`)는 이걸 돌려야 실제로 지워진다. 집에서 시험할 때 올라간 사진 `test-001.jpg` 도 여기서 지워진다 | 파일을 통째로 붙여넣고 Run. 여러 번 실행해도 안전하다 |
| 2 | **박스 갱신** — `문서/박스업데이트.md` 절차대로 새 코드(`a6320ef`) 받기 | 박스는 아직 옛 코드로 돌고 있어서 좌석 파일이 바뀌면 카메라 사진을 올린다. 갱신해야 실제로 멈춘다 | 갱신 뒤 시작 화면에 `이름표 사진:` 줄이 **없어야** 정상 |
| 3 | **신청서 수정 요청 보내기** — `문서/신청서수정요청.md` 를 작성자에게 | 신청서의 "좌석ID+점유여부+시각뿐" 이 사실과 다르다. 판정 개선용 수치 기록(순간 기록·하루 요약)과 3상태 판정, 자세 모델을 적어야 한다 | 그 파일 내용을 그대로 보낸다 |

## 설치 때 (문큐)

- **배치 사진은 사람이 폰으로 찍는다.** 박스는 어떤 사진도 올리지 않는다.
  자리마다 이름표(`T1`, `BAR7-1` …)를 써 넣어 앱 팀에게 메신저로 보낸다.
- **이름표 목록**은 Claude 에게 "이름표 목록 뽑아줘" 라고 하면 좌석 파일에서 뽑아 준다. 사진과 같이 보낸다.
- 나머지 절차는 `문서/카페설치당일.md`.

## 그 다음 (판정 개선 — `문서/판정개선_데이터설계.md` §10-6)

1. **버린 물건도 기록에 남기기.** 규칙에 걸려 버려진 짐이 지금은 기록에서 사라진다. 받는 쪽은 이미 준비돼 있고 엔진이 넣어 주기만 하면 된다. 판정에 영향 없음 → 재채점 불필요.
2. **`novelty` (빈 상태와 얼마나 달라졌나).** 지갑처럼 이름 없는 물건까지 잡는 유일한 신호. **판정에 영향을 주므로 72칸 정답지로 다시 채점한다** (`CLAUDE.md` 속도·채점 규칙).
