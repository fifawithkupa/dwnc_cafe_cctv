# SeatNow 코드 작업 플랜

> 작성: 2026-08-26 / 기준 커밋: `main` = `9c0aa4e`
> 갱신: 2026-08-27 — T13(seat_report 출력 계약) 구현 완료. 보유 영상 현황 반영
> 갱신: 2026-08-31 — T9(하드웨어 디코딩)·T17(디코딩 벤치) 완료. **카메라 선정 근거 정정** (§2)
> 갱신: 2026-09-01 — T14 완료(검출 검사 하네스, §0-c), 2D 평면도 1·2단계 완료(§0-d)
> 갱신: 2026-09-01(2) — **지도는 출력이라는 결정**, 저장소 폴더 재배치, 평면도 편집기 실사용 가능 (§0-e)
> 갱신: 2026-09-01(3) — angle1 12장 Codex 채점 완료, **사람 눈 검수 폴더** (§0-f)
> 근거: 저장소 코드 감사 + 하드웨어 구매 감사 결과 (`SEATNOW_전체정리.md` §9 참조)
> 프로젝트 규칙: **`CLAUDE.md`를 먼저 읽을 것** — 카메라 설치 주체, 수작업 예산, 3상태 출력 원칙

---

## 0-a. 진행 상황 (2026-08-31)

| 작업 | 상태 | 비고 |
|---|---|---|
| T1 회귀 복구 | ✅ 완료 | 방향 **(a) 의자 연결 복구** 채택. `tests/test_analyze_pipeline.py` 신설 |
| T2 진단 로깅 | ✅ 완료 | `--log-detections` |
| T3 평가셋 | ⏳ **코드 완료 / 라벨 대기** | `checks/make_labels.py` + 다중 fixture 채점. **라벨링은 사람 작업** |
| T4 익스포트+벤치 | ✅ 완료 | `edge/export.py`, `edge/bench.py`. **2026-08-31 노트북 baseline 실측 완료** |
| T5 RTSP 재송출 | ✅ 완료 | `edge/rtsp_republish.py` (mediamtx 설치 필요) |
| T6 엣지 벤치 | ⛔ 하드웨어 대기 | 도구 준비 완료 — 엣지에서 `edge/check_edge.py` → `edge/bench.py` → `edge/bench_decode.py` |
| T7 파라미터 스윕 | ⏳ 하네스 완료 / 실행 대기 | `edge/bench_sweep.py`. T3 라벨 + T6 예산 필요 |
| **T9 Quick Sync** | ✅ **완료 (2026-08-31)** | `--hwaccel`. **T17보다 먼저 해야 했다** — 아래 참조 |
| T8·T10 | ⬜ 미착수 | **카메라 확정 후.** 카메라 없이는 검증이 불가능하다 |
| T11 파인튜닝 | ⬜ 조건부 | T7 결과에 따라 판단 |
| T12 문서 | ✅ 완료 | README·전체정리 갱신 |
| **T13 출력 계약** | ✅ **완료 (2026-08-27)** | `seat_report` + 바 구역 좌석 칸 + UNKNOWN 사유 코드. 아래 참조 |
| **T14 실영상 첫 실행** | ✅ **완료 (2026-09-01)** | 하네스로 angle1~4 완주 + angle1 레이아웃 비교. §0-c 아래 |
| **2D 평면도 1단계** | ✅ **완료 (2026-09-01)** | 스키마 v3(소속 미정 의자·바닥 네 점) + `install/calibrate.py` [f]/[m]/[g]. 설계: `docs/superpowers/specs/2026-09-01-2d-floorplan-design.md` |
| **2D 평면도 2단계** | ✅ **완료 (2026-09-01)** | 호모그래피 투영 + `floorplan.json` + 브라우저 편집기. §0-d |
| **T17 디코딩 벤치** | ✅ **완료 (2026-08-31)** | `edge/bench_decode.py`, `edge/check_edge.py`, `docs/edge-setup.md` |
| **2D 평면도 3단계(편집기 실사용)** | ✅ **완료 (2026-09-01)** | 확대·이동, 벽/입구/카운터/소파/창문, 회전·크기, 2인 의자, 의자 숨기기. 버그 3개 수정. §0-e |
| **저장소 재배치** | ✅ **완료 (2026-09-01)** | `engine`/`install`/`checks`/`edge`/`results`. §0-e |
| **angle1 12장 채점** | ✅ **완료 (2026-09-01)** | Codex 채점 5장→12장. §0-c 끝 |
| **angle1 사람 눈 검수** | ⏳ **진행 중** | `results/angle1_layout/review/` — 사진 12장을 사람이 한 장씩 확인. §0-f |
| **평면도 실제 그리기** | ⏸ **보류** | 편집기는 다 됐다. 방을 실제로 그리는 작업만 미뤘다 |

~~**⚠️ 아직 한 번도 실제 영상으로 돌려본 적이 없다.**~~ → **2026-08-31 해소.**
`torch 2.2.2` + `ultralytics 8.4.82` 설치 완료(Python 3.11.9/Windows), 테스트 531개 통과(2026-09-01 기준),
angle1~4에 `_preseed`를 돌려 모델이 실제로 무엇을 보는지 처음 확인했다 (§0-c).

---

## 0-b. 보유 영상 현황 (2026-08-27 실측)

`sample_raw/` — 저장소에 없음(`.gitignore`), 총 1.6GB

| 파일 | 해상도 | 길이 | fps | 용도 |
|---|---|---:|---:|---|
| `cafe_sample_angle1.mov` | 1920×1080 | 78초 | 30 | **각도 선정** |
| `cafe_sample_angle2.mov` | 1920×1080 | 63초 | 30 | **각도 선정** |
| `cafe_sample_angle3.mov` | 1920×1080 | 70초 | 30 | **각도 선정** |
| `cafe_sample_angle4.mov` | 1920×1080 | 78초 | 30 | **각도 선정** |
| `cafe_sample_그냥착석.mov` | 1920×1080 | 10.9초 | 30 | 데모 전용 |
| `cafe_sample_멀리앉기.mov` | 1920×1080 | 8.8초 | 30 | 데모 전용 |
| `cafe_sample_의자옮김.mov` | 1920×1080 | 12.6초 | 30 | 데모 전용 |
| `moonq_sample_longform.mp4` | **1280×720** | 631초 | **120** | 성격 확인 필요 |

**여기서 T3의 전제가 틀렸다는 게 드러난다.** T3은 "1시간 영상 + 엣지케이스 10분"을
전제로 썼는데, 실제로는 그런 영상이 없다.

1. **시나리오 3개(9~13초)는 채점에 쓸 수 없다.** tick 15초보다 짧아 판단이 0~1회만
   일어난다. 눈으로 보는 데모용이다 (`--sample-seconds 5`로 줄여 실행).
2. **angle1~4는 합쳐서 4.8분, 기본 tick 기준 약 18틱이다.** 파라미터 A/B 차이를
   가릴 통계력이 없다. 이건 평가셋이 아니라 **설치 각도 선정용**으로 쓴다.
3. **시나리오 3개는 서로 같은 각도다** (사용자 확인). 레이아웃 JSON 하나로 커버된다.
4. `moonq_sample_longform`은 720p/120fps로 다른 넷(1080p/30fps)과 촬영 장비가 다르다.
   **같은 카페의 고정 촬영인지 확인 필요** — 맞다면 유일한 실질 평가셋 후보다(10.5분,
   약 42틱). 아니면 각도 확정 후 재촬영해야 한다.

**상태 전이는 어느 영상으로도 거의 못 잡는다.** 이탈 확정에 3틱=45초가 필요하고
(`occupy_confirmations=2`, `empty_confirmations=3`), 78초 영상에는 완전한
"앉았다 일어남" 사이클(약 65초)이 한 번 겨우 들어간다. fixture의 `expected_events`
채점은 재촬영 전까지 비워둔다.

---

## 0-c. 2026-08-31 실측 — 모델·하드웨어 첫 숫자

### 모델이 이 카페를 얼마나 보는가 (T14 일부)

`_preseed`(yolov8x @1280)를 angle1~4의 5초 지점 프레임에 돌린 결과.

| 각도 | 잡은 테이블 | 연결된 의자 | 의자가 붙은 테이블 |
|---|---:|---:|---|
| angle1 | 7 | 17 | 6/7 |
| angle2 | 4 | 15 | 4/4 |
| angle3 | 6 | 16 | 4/6 |
| angle4 | 4 | 17 | 3/4 |

**드러난 것 3가지**

1. **일반 테이블은 잘 잡는다.** 각도마다 4~7개. 의자 연결도 대체로 붙는다.
2. **일자형/벽 책상이 실물로 확인됐다.** angle2 왼쪽 벽면의 긴 벤치 + 긴 목재
   책상을 모델은 **테이블 1~2개로** 본다. 6석짜리 자리가 상태 1개로 뭉개진다 —
   **T13의 좌석 칸(`counted_zone` + `seats`)이 필요한 이유가 실물로 증명됐다.**
   설치 검수 때 `[z]`+`[x]`로 사람이 칸을 그어야 한다.
3. **의자 과탐이 심하다.** 같은 의자에 상자가 여러 개 겹쳐 잡힌다. 캘리브레이션
   에서 지우는 것이 `CLAUDE.md`가 허용한 수작업 범위 안이므로 코드로 풀지 않는다.

> 열린 질문 2번("일자형/벽 책상이 몇 개, 몇 자리인가")은 이제 **angle2 왼쪽 벽면**
> 으로 특정됐다. 자리 수는 설치 검수 때 사람이 세어 칸으로 긋는다.

### 2026-09-01 — 검출 검사 하네스 첫 실행 (T14 완결)

**판정 파이프라인이 실제 영상에서 처음으로 끝까지 돌았다.** 8/31에 한 것은
`_preseed`를 각 각도의 한 장에 돌린 것뿐이었다.

angle1~4를 **레이아웃 없이**(모델만 놓고 보기 위해) `yolov8n`으로 15초 주기로
돌려 30 tick, 사진 60장. Codex가 깨끗한 사진만 보고 사람과 테이블을 셌고,
그중 5장은 사람이 따로 세어 대조했다.

판독표: `results/angle{1..4}.md` · 대조: `results/human_spotcheck/report.md`
도구: `engine/seatnow.py --frame-dir` + `checks/judge_frames.py` + `checks/inspect_run.py`
설계: `docs/superpowers/specs/2026-09-01-detection-inspection-harness-design.md`

| 각도 | tick | 검출 재현율 | 포즈 재현율 | 검출 과탐 | 포즈 과탐 | 자리 없음 부풀림 |
|---|---:|---:|---:|---|---|---|
| angle1 | 6 | 1.00 | 1.00 | 4/6장 +5명 | 1/6장 +1명 | 정답 없음 |
| angle2 | 7 | 1.00 | 0.93 | 2/7장 +2명 | 1/7장 +1명 | 0/1장 (정확) |
| angle3 | 9 | 1.00 | 1.00 | 7/9장 +13명 | 3/9장 +4명 | **3/3장 +5석** |
| angle4 | 8 | 1.00 | 0.94 | **8/8장 +17명** | 1/8장 +1명 | 정답 없음 |

UNKNOWN은 **네 각도 전부 0건**이다. IGNORE도 0건이다.

#### 결론 한 문장

> **모델은 사람을 하나도 안 놓친다(재현율 1.00). 검출기는 심하게 과탐하지만
> (총 +37명) 그 대부분은 포즈 층이 이미 걸러낸다(총 +7명). 무너지는 곳은 그
> 다음 층이고, 하필 거기가 이 하네스로 아직 못 재는 곳이다.**

#### 근거

1. **놓치는 문제는 없다.** 30장 전부에서 사람을 다 찾았다. 실패 방향이
   예상과 반대여서 **재현율만 봤으면 "다 잘 된다"로 잘못 결론냈을 것이다** —
   재현율은 찾은 수를 정답에서 자르므로 2명 장면에서 7명을 잡아도 1.00이다.
   그래서 과탐 지표를 추가했다
2. **검출기 과탐은 파이프라인이 대부분 버린다.** 검출기 +37명 대비 포즈 층
   +7명. 좌석 판정에 쓰이는 것은 포즈 층이므로 **검출기 원본의 과탐을 그대로
   제품 결함으로 읽으면 안 된다**
3. **점유의 근거가 사람이 아니다.** 판정 128건 중 `belongings` 60건(47%),
   `no_customer_evidence` 37건(29%), `track_predicted` 25건(20%),
   **`person_seated`는 4건(3%)**. 이것은 결함이 아니라 설계다
   (`engine/seatnow_core.py:890-906`, `CLAUDE.md`의 "구제 경로가 주 경로")
4. **그 짐 판정이 맞는지는 아직 모른다.** 30장 중 짐을 채점할 수 있었던 것은
   **4장뿐**이다(§아래 한계). 그 4장 중 angle3의 3장은 **전부 부풀림**이고
   없는 점유가 총 5석, 최악 한 장 +3석이다. 표본이 3장이라 결론이 아니라 단서다

#### 다음 행동 — 파인튜닝이 아니다

`model` 그룹 UNKNOWN **0건**, `geometry` **0건**, IGNORE **0건**. T11(파인튜닝)의
발동 조건도, 카메라 각도를 다시 고를 근거도 여기서 나오지 않는다.

1. **T14 레이아웃 그리기가 진짜 병목이 됐다.** 대조에서 사람과 Codex가 갈린
   원인이 전부 **창가 일자형 카운터**였다 — 사람은 판정 단위가 아니라고 빼고,
   Codex는 테이블로 셌다. `[z]`+`[x]`로 자리 칸을 긋기 전에는 이 구역의
   점유를 아무도 채점할 수 없다
2. **짐 정답은 자동으로 못 얻는다.** 다음 판독은 질문을 바꾸거나 사람이 센다

#### 이 실행이 답하지 못하는 것

- **짐 정답이 26/30 불확실이다.** Codex 메모가 전부 같은 말이었다 — "회색 물건이
  손님 짐인지 카페 비품인지 확실하지 않다". **정지 사진 한 장으로는 손님 짐과
  카페 비품이 원리적으로 잘 안 갈린다.** 우리 코드도 같은 구분을 하고 있고,
  점유의 47%가 거기 걸려 있다
- **정답이 실행마다 흔들린다.** `angle2/t0015.0s`에 Codex가 1차 3명, 3차 2명으로
  답했고 **두 번 다 "확실하다"고 했다.** 그 한 장이 angle2 재현율을 0.92↔1.00으로
  뒤집었다. 정답으로 쓰려면 같은 사진을 여러 번 물어 다수결을 하거나, 어긋나는
  장만 사람이 확인해야 한다
- **영상에 손님이 상시 2명뿐이다.** 붐빌 때는 못 쟀다. 가림이 심해지는 것은
  사람이 많을 때인데 그 조건이 이 영상에 없다
- **좌석 판정 정확도(%)는 안 쟀다.** 좌석별 라벨(T16)이 필요하다
- **자세(앉음/섬)는 정답이 못 된다.** 대조 5장 중 1장에서 사람과 Codex가 갈렸고
  원인은 **다른 의자보다 높은 의자**였다(사용자 확인). 우리 코드의 앉음 판정도
  각도 임계값(`--angle 110`)이라 같은 함정을 밟는다
- **의자 검출은 프레임당 18~42개다.** §0-c의 의자 과탐이 실측으로 확인됐다.
  `CLAUDE.md`대로 캘리브레이션에서 사람이 지우고 코드로 풀지 않는다

---

### 2026-09-01 — 사람이 그린 레이아웃을 넣었을 때 (T14 완결)

`layouts/cafe_angle1.json`을 그려 angle1을 다시 돌렸다. 판독표는
`results/angle1_layout/report.md`(레이아웃 있음)와 `results/angle1/report.md`(없음).

| tick당 평균 | 레이아웃 없음 | 레이아웃 있음 |
|---|---:|---:|
| 총 좌석 | 4.0 | **12.0** |
| 사용 중 | 2.7 | 3.0 |
| **빈자리** | **1.3** | **8.8** |
| 모름 | 0.0 | 0.2 |

**가장 큰 변화는 정확도가 아니라 존재다.** 레이아웃 없이는 이 카페가 4석으로
보였고 그중 1.3석만 비어 있었다. 실제로는 12석이고 8.8석이 빈다. **창가 6석이
판정 대상에 아예 없었기 때문**이며, §0-c가 예고한 "6석짜리 자리가 상태 1개로
뭉개진다"가 그대로였다. 사람이 칸을 그어야만 세어진다.

부수적으로 판정도 안정됐다.

| | 없음 | 있음 |
|---|---|---|
| 포즈 과탐 | 1/6장 (+1명) | **0/5장** |
| `track_predicted` | 5건 | **0건** |
| 검출 재현율 | 1.00 | 1.00 |

`track_predicted`가 사라진 것은 자리가 고정됐기 때문이다. 레이아웃이 없으면
테이블이 검출에서 깜빡할 때마다 추적으로 버텨야 했다.

새로 보이는 것은 `spans_multiple_seats` 2건 — 한 사람의 박스가 창가 자리 칸
두 개에 걸쳐 둘 다 UNKNOWN이 된 경우다. 12틱 × 6칸 = 72회 중 2건(약 3%).
손님이 늘면 커질 부분이고, 칸을 넓게 그으면 줄어든다.

#### 바 자리 규칙을 바꿨다 — 짐만으로는 점유하지 않는다

바 카운터에서는 **한 손님이 노트북·컵·가방을 스툴 두세 개 폭으로 펼친다.**
칸마다 `objects`가 보여서 한 명이 세 자리를 먹는다. 테이블은 통째로 하나라
생기지 않는 문제다. 옆 스툴의 짐은 치워달라면 대부분 치워주므로 실제로는 쓸 수
있는 자리다 (사용자 판단, 2026-09-01).

- **테이블은 그대로 짐 = 점유**다. 이 규칙은 `counted_zone` 자리에만 적용된다
- **자세를 못 읽은 사람이 있으면 바에서도 UNKNOWN**이다. 누군가 있는데
  "비었음"으로 반올림하면 손님을 남의 자리로 보낸다
- 짐을 보고도 안 셌을 때는 사유를 `belongings_only`로 남긴다. 정말 빈 자리와
  같은 코드로 뭉치면 이 선택이 맞았는지 나중에 잴 수 없다

**이 영상에서는 한 번도 발동하지 않았다** (`belongings_only` 0건). 창가에 짐이
놓인 장면에서 모델이 그 짐을 못 잡았기 때문이다 — 아래.

#### 처음 잡은 짐 미검출

75초 프레임의 **창가 맨 오른쪽 자리에 검은 가방이 실제로 있는데 모델이 어떤
확신도로도 잡지 못했다.** 그 칸 주변에 나온 것은 의자와 테이블뿐이고 가방 후보는
0개였다 (`--object-conf 0.15`).

지금까지의 결론은 "사람은 하나도 안 놓친다"였는데, **짐은 놓친다.** 판독에서
짐 정답이 30장 중 4장만 채점 가능했던 것과 같은 방향이다.

**다만 이 건은 결과를 바꾸지 않았다.** 바 자리였고, 새 규칙에서 바 자리의 짐은
어차피 점유를 만들지 않는다. 짐 미검출이 실제로 아픈 곳은 **테이블**이며,
거기서는 짐이 여전히 점유의 47%를 지탱한다.

#### 2026-09-01(2) — 표본을 5장에서 12장으로 늘렸다

위 숫자는 **사진 5장** 기준이었다(15초 틱이던 옛 실행의 Codex 채점을 빌려 씀).
남은 7장을 채점해 **12장 전부**로 다시 냈다. 판정은 그대로다 — 같은 레이아웃으로
다시 돌려 12틱의 점유/빈/모름이 어제와 **완전히 동일**한 것을 확인했다
(폴더 재배치가 파이프라인을 안 깼다는 증거이기도 하다).

| | 5장 | **12장** |
|---|---:|---:|
| 검출 재현율 | 1.00 (10/10) | **0.96 (23/24)** |
| 포즈 재현율 | 1.00 (10/10) | 1.00 (24/24) |
| 검출 과탐 | 3/5장 (60%) | 4/12장 (33%) |
| 포즈 과탐 | 0/5장 (0%) | **1/12장 (8%)** |
| 좌석 점유 채점 | 정답 없음 | **0/2장 부풀림 (정확 2장)** |

**"사람은 하나도 안 놓친다"가 처음 깨졌다.** `t0035.0s`에서 실제 2명 중
검출기가 1명만 잡았다. **다만 포즈 모델은 2명을 다 잡았고**, 판정은 포즈를
쓰므로 좌석 결과는 틀리지 않았다. 검출 재현율 0.96 / 포즈 재현율 1.00 의
차이가 이것이다.

**좌석 판정 자체는 안정적이다.** 12틱 중 11틱이 `점유 3 / 빈 9 / 모름 0`,
15초 시점만 `점유 3 / 빈 7 / 모름 2`. 채점 가능했던 2장에서 점유 수가
정확히 맞았다.

**짐은 채점이 거의 불가능하다.** 12장 중 10장이 `uncertain_tables` —
Codex가 놓인 물건이 손님 짐인지 카페 비품인지 가리지 못했다. 테이블 점유의
47%를 짐이 지탱하는데 그 부분을 **잴 방법이 없다**는 뜻이고, T16(좌석별
라벨링)이 필요한 이유가 여기서 다시 나온다.

---

## 0-d. 2026-09-01 — 손님용 2D 평면도 (2단계 완료)

`seat_report`에는 상태만 있고 **위치가 없었다.** 앱이 "3자리 남음"은 찍어도 어느
자리인지 못 보여준다. 그 빠진 절반을 만들었다.

설계: `docs/superpowers/specs/2026-09-01-2d-floorplan-design.md`
계획: `docs/superpowers/plans/2026-09-01-floorplan-stage2-editor.md`

| 파일 | 하는 일 |
|---|---|
| `install/floor_projection.py` | 바닥 네 점 → 호모그래피. 꼬인·좁은 사각형은 거부 |
| `install/floorplan.py` | `floorplan.json` 초안. 의자·바 배치 규칙, 겹침 해소 |
| `install/floorplan_editor.py` | 로컬 서버. 저장 시 **평면도와 레이아웃을 함께** 갱신 |
| `static/floorplan_editor.html` | 건축 도면풍 편집 화면 |

**설치 절차가 두 칸 늘었다.** `install/calibrate.py`(3-a~3-d) 다음에 바닥 네 점을 찍고,
브라우저에서 평면도를 다듬는다. 엣지 박스가 카페 와이파이로 페이지를 띄우므로
노트북이든 폰이든 아무거나로 열면 된다.

### 판정에 영향을 주는 것은 의자 소속 하나뿐이다

지도는 보여주기용이고 판정은 카메라 화면 좌표로 한다. 예외가 **의자 소속**인데,
손으로 그린 연결은 판정에서 무조건 참으로 쓰인다(`engine/seatnow_core.py:1531-1534`).
그래서 편집기 저장은 **두 파일을 원자적으로** 갱신한다 — 반만 저장되면 지도와
판정이 어긋난 채로 아무도 모른다.

### 그리는 규칙 (투영값을 그대로 쓰지 않는다)

투영된 의자 위치는 흩어져서 도면으로 읽히지 않는다. 손님이 읽는 것은 "이 테이블이
몇 인석인가"이고, 판정은 어차피 이 좌표를 안 본다.

- **의자는 테이블 둘레에** 위 → 아래 → 위 → 아래 → 오른쪽 → 왼쪽 순으로 붙인다.
  5개면 위 2 / 아래 2 / 오른쪽 1이 되고, 실제 T6이 그 모양이다
- **바는 긴 테이블 하나**다. 스툴은 그 옆 방 쪽에 동그라미로 균등 배치한다.
  바 구역에 속한 의자는 따로 그리지 않는다 — **스툴이 좌석이자 의자**이고 둘 다
  그리면 여섯 자리가 열두 자리로 보인다
- **겹치면 밀어낸다.** 테이블은 의자 자리만큼 부풀려 검사하고, 비스듬한 카운터는
  작은 상자를 이어붙여 실제 모양으로 본다. 실측 angle1에서 12건 → 0건

### 아직 사람이 해야 하는 것 — 벽

**카메라는 좌석만 보지 벽은 안 본다.** 데이터가 없으므로 `[벽 그리기]`로 방 둘레를
한 번 그려야 한다. 좌석이 이미 놓여 있으니 그 둘레를 따라 8~10번 클릭이면 된다.
벽이 없으면 가구가 허공에 뜬 그림이 되어 손님이 못 읽는다.

### 4단계(바닥 좌표로 판정)는 T16 다음이다

판정을 화면이 아니라 **바닥 좌표**에서 하면 사람↔좌석 연결, 두 칸 걸침, 지나가는
사람 구분이 다 좋아진다. 그런데 **좋아졌는지 잴 수가 없다** — 좌석별 라벨(T16)이
없고, 지금 하네스는 사람 검출 재현율과 과탐만 낸다.

대신 2단계가 재료를 남긴다. `floorplan.json`의 좌석마다 `image_anchor`(투영에 쓴
화면 좌표)를 싣는다. 사람이 지도를 고치고 나면 좌석 수만큼 대응쌍이 생기므로,
처음 손으로 찍은 **네 점보다 훨씬 나은 호모그래피를 다시 맞출 수 있다.**

---

### 이 노트북 baseline (엣지와 비교할 기준선)

`edge/bench.py --backends pt` — 8코어 최신 랩탑, PyTorch 백엔드(OpenVINO 미익스포트).

| profile | 추론/tick | tick 소요 | tick 사용률 | 판정 |
|---|---:|---:|---:|:---:|
| accuracy_default | 30 | 3.9s | 26% | PASS |
| balanced | 20 | 1.9s | 13% | PASS |
| fast | 10 | 0.9s | 6% | PASS |

 `edge/bench_decode.py` — 디코딩 비용(영상 1초당 CPU 초 = 상시 점유 코어).

| 클립 | 소프트웨어 | 하드웨어(d3d11va) |
|---|---:|---:|
| 2MP H.264 | 0.17 | 0.05 |
| 4MP H.265 | 0.38 | 0.07 |
| 8MP H.265 | 0.76 | 0.14 |

**여기서 나온 결론 2가지**

1. **하드웨어 디코딩이 비용을 3~6배 줄인다.** 그래서 T9를 T17보다 먼저 했다.
   안 켜고 재면 살 수 있는 카메라를 못 산다고 잘못 판단한다.
2. **H.265는 소프트웨어에서만 비싸다.** H.264 대비 소프트웨어 +40%,
   하드웨어에서는 차이 없음. **드라이버를 못 잡는 박스라면 H.264 카메라를 사야
   한다**는 구매 조건이 여기서 나온다.

> 이 노트북은 전부 PASS다. **엣지 i3에서 다시 재야 의미가 있다** — 그게 T6다.

---

## 0-e. 2026-09-01(2) — 지도는 출력이다 / 폴더 재배치 / 편집기 실사용

설계: `docs/superpowers/specs/2026-09-01-map-as-output-and-supabase-design.md`

### 결정 — 바닥 좌표로 판정하지 않는다 (§0-d 4단계 폐기)

지도는 **사람이 보기 좋으라고 손으로 고치는 그림**이다. 그게 판정 입력이 되면
매장에서 "보기 좋게 옮겼을 뿐"인 조작이 손님 앱의 빈자리 숫자를 바꾼다.
그래서 **판정 입력은 `calibrate.py`가 만든 레이아웃 하나로 고정**한다.

- 허용되는 방향: 지도에서 고친 **의자 소속** → 레이아웃 → 판정
- 막는 방향: 지도 좌표 → 판정. `tests/test_judgement_inputs.py` 가 고정한다
  (판정 모듈이 평면도를 import 하면 실패)

### 저장소를 하는 일별로 나눴다

| 폴더 | 언제 쓰나 |
|---|---|
| `engine/` | 판정. 매장에서 실제로 도는 코드 |
| `install/` | 설치 당일 한 번. 캘리브레이션 + 2D 평면도 |
| `checks/` | 채점 (판독표·정답 대조·라벨링) |
| `edge/` | 엣지 박스 성능 재기 |
| `results/` | **결과 전부.** 영상 하나 = 폴더 하나 |

실행은 `python -m engine.seatnow ...` 형태로 바뀌었다. 결과 폴더 하나에
`report.md`(판독표) / `log.jsonl` / `clean` / `marked` / `judge` 가 다 들어간다.
저장소에는 `report.md` 와 Codex 정답지만 올린다.

죽은 시제품 `occupancy_mvp.py`·`pose_judge.py` 삭제.

### 편집기에서 잡은 버그 3개 (전부 회귀 테스트 포함)

1. **클릭이 지도 밖으로 떨어졌다.** 지도는 세로로 긴 752×1160 인데 화면 칸은
   가로로 넓어서 브라우저가 세로에 맞춰 0.690배로 줄이고 좌우에 701px 여백을
   둔다. 코드는 가로 기준 배율(2.553)만 쓰고 여백을 빼지 않았다. 어디를 눌러도
   아무것도 안 잡혔다. → SVG 자신의 행렬(`getScreenCTM`)에 묻는다
2. **바가 저 혼자 떠내려갔다.** 바 선을 이미 밀려난 스툴에서 뽑고 다시 밀어내
   한 번 돌 때마다 75단위씩 누적. 편집기는 드래그마다 재배치를 부르므로,
   테이블을 옮기면 바가 걸어서 도망갔다. → 스툴이 선 위에 남고 카운터가 물러난다
3. **평면도를 다시 열면 의자 소속 변경이 반영 안 됐다.** 저장 시 좌표를 소수
   둘째 자리로 반올림하는데 대조는 원래 정밀도로 해서, 19개 중 7개가 열쇠에
   안 걸렸다. 지도에는 옮겨진 것으로 보이고 판정은 옛 소속을 썼다

### 편집기에 넣은 것

확대·축소(휠/버튼), 화면 이동, 벽을 네모 끌기로 그리기, 입구·카운터·소파·창문,
테이블/의자/구역의 **회전·크기 조절**, 원형/사각 전환, 2인 의자 표시,
의자를 **지도에서만** 빼기(판정에서는 유지 — 레이아웃의 의자는 판정에 확정
검출로 들어가 가려진 사람을 구제하고 짐을 잡는다, `engine/seatnow_core.py:1449-1452`).

평면도 스키마 2 (좌석 angle/shape, 의자 capacity/angle/hidden, 랜드마크 angle).
스키마 1 파일도 그대로 열린다.

### 아직 안 한 것

- **`layouts/cafe_angle1.floorplan.json` 이 아직 없다.** 편집기를 실사용
  가능하게 만드는 데 시간을 썼고, 실제로 방을 그리는 작업은 미뤘다
- **바닥 기준 네 점이 잘못 찍혀 있다.** 4점 중 2점이 흰 테이블 **상판** 위에
  있다. 호모그래피는 평면 하나만 펴는 계산이라 바닥 2점 + 상판 2점을 섞으면
  결과가 부정확한 게 아니라 **무의미**하다. 그래서 T4·T5·바가 엉뚱한 곳에
  그려진다. 지금은 그대로 두고 편집기에서 손으로 맞추기로 했다
  (마루 이음매로 직각을 자동 계산해 봤으나 나뭇결이 섞여 실패했다)
- Supabase 연동 — 스키마만 확정, 구현 안 함

### 좌석을 세는 단위 (앱 문구에 영향)

`engine/seatnow_report.py:160` — **테이블은 의자가 몇 개든 `capacity: 1`**이다.
바만 사람이 그은 칸 수만큼 센다. 그래서 이 카페의 "총 12자리"는 실제 인원이
아니라 **테이블 6 + 바 6**이다. 앱 문구는 "3자리 남음"이 아니라
**"빈 테이블 3개"**가 정직하다 (결정 대기).

---

## 0-f. 2026-09-01(3) — 사람 눈으로 한 장씩 검수하는 폴더

Codex 채점은 기계가 기계를 채점한 것이다. **angle1은 12장뿐이라 사람이 전부
볼 수 있고**, 그게 지금 있는 정답지 중 가장 믿을 만하다. 한 장씩 넘겨보게
만들었다.

```
results/angle1_layout/review/
  00_읽는법.md
  01_t0000s_점유3빈9모름0_사람3대2_차이+1_★_원본.jpg
  01_t0000s_점유3빈9모름0_사람3대2_차이+1_★_판정.jpg
  ... (원본 12 + 판정 12)
```

**파일 이름이 곧 요약이다** — 몇 초 지점인지, 우리 좌석 판정이 무엇인지,
우리가 센 사람과 Codex가 센 사람이 몇 대 몇인지, 어긋나면 `★`.
`_원본`이 `_판정`보다 먼저 정렬된다 — 답을 보기 전에 직접 세어 보라고.

`_판정` 그림은 **다시 그렸다.** 원래 주석 사진은 `layout=v1 ACTIVE conf=1.00
table=1.00 | no_customer_evidence` 같은 긴 글자가 서로 겹쳐 화면을 덮어 사람이
읽을 수가 없었다. 자리 이름과 상태만 남겼다 — 빨강 `X T1`(사용중) /
초록 `O T3`(빈자리) / 회색 `? T5`(모름) / 파랑 `person`.

`00_읽는법.md` 에 틱마다 **자리별 판정과 그 이유를 한글로** 적었다
("T1 사용중 / 짐이 있음", "T3 빔 / 아무 근거 없음").

### 확인 대상 5장

| 파일 | 우리 : Codex |
|---|---|
| `01_t0000s` | 3 : 2 (+1) |
| `05_t0030s` | 4 : 2 (+2) |
| **`06_t0035s`** | **1 : 2 (−1) — 놓친 쪽** |
| `07_t0040s` | 3 : 2 (+1) |
| `08_t0045s` | 3 : 2 (+1) |

`06_t0035s` 는 이미 확인했다. **안쪽 창가에 앉은 사람에게 파란 상자가 없다.**
검출 재현율을 1.00에서 0.96으로 떨어뜨린 그 한 명이다. 포즈 모델은 그 사람을
잡았고 판정은 포즈를 쓰므로 **좌석 결과는 틀리지 않았다.**

### 채점 도구에 붙인 것

`checks/judge_frames.py --skip-existing` — 이미 채점된 사진은 다시 묻지 않는다.
Codex 가 보는 것은 사진이고 사진은 실행을 반복해도 그대로다. 이번에도 두 실행의
사진 5장이 **해시까지 완전히 같아** 그대로 재사용했고 7장만 새로 물었다.
저장된 것이 실패 기록이면 다시 시도한다 — 실패를 건너뛰면 구멍이 영영 남는다.

### 그림 그릴 때 걸린 것 (윈도우)

`cv2.imwrite` 는 **한글이 든 경로에 쓰지 못하고 예외 없이 False 만 돌려준다.**
12장을 다시 그렸다고 출력해 놓고 실제로는 한 장도 안 바뀌어 있었다.
`cv2.imencode` + `Path.write_bytes` 로 바꾸고, 결과 높이가 요약 띠만큼
늘었는지 다시 읽어 확인했다.

### 사람이 확인해야 답이 나오는 것

**짐은 Codex가 12장 중 10장에서 "손님 짐인지 카페 비품인지 모르겠다"고 답했다.**
테이블 점유의 47%를 짐이 지탱하는데 그 부분의 정답지가 없다. 지금으로서는
**사람 눈이 유일한 정답지**이고, 이 검수가 T16(좌석별 라벨링)의 예고편이다.

---

## 0. 현재 상황

| 항목 | 상태 |
|---|---|
| 판정 로직 | 완성도 높음. ~~§9 회귀~~ → **T1로 복구됨** |
| 영상 입력 | **로컬 파일 전용.** RTSP 경로 없음 (`engine/seatnow.py:118`, `engine/seatnow_core.py:2760`) — T8에서 처리 |
| 모델 배포 | ~~익스포트 경로 없음~~ → ** `edge/export.py`로 OpenVINO FP32/INT8** |
| 배포 준비 | Dockerfile·systemd·CI **전부 없음** — T10 |
| 평가 | ~~fixture 1개(20초)~~ → 다중 fixture 채점 + 커버리지 집계 가능. **라벨 입력 대기** |
| 하드웨어 | 엣지 박스(중고 미니PC, Intel 6세대 i3급, ~10만원) 구매 진행 중. **카메라는 벤치 결과 후 구매** |
| 카메라 대수 | **1대 확정** |

---

## 1. 의존 관계

```
T1 회귀 복구 ──┬──> T3 평가셋 ──> T7 파라미터 스윕 ──┬──> T11 파인튜닝(조건부)
               │                                     │
T2 진단 로깅 ──┘                  T6 엣지 벤치 ──────┘
                                        ▲
T4 익스포트+벤치 도구 ──────────────────┤
T5 RTSP 재송출 하네스 ──────────────────┘
                                        │
                                        └──> T8 RTSP 리더 ──> T9 QSV ──> T10 배포 프로파일

T13 출력 계약 ──> T14 실영상 첫 실행 ──> T15 설치 각도 선정 ──> T16 라벨링 ──> T7
```

**핵심 제약 1: T1이 모든 측정의 선행 조건이다.** 기준선이 무너진 상태에서 스윕·파인튜닝·정확도 비교를 하면 전부 무의미하다. → T1 완료됨.

**핵심 제약 2: T15(각도 확정)가 T16(라벨링)의 선행 조건이다.** 각도가 안 정해진
상태에서 라벨을 만들면, 각도가 바뀔 때 그 노동이 통째로 버려진다. 라벨링은 이 플랜
에서 가장 비싼 사람 노동이므로 순서를 지킬 것.

---

## Phase 1 — 기준선 복구 (구매 불필요, 지금 착수)

### T1. 의자 연결 회귀 복구 ⚠️ 최우선

**문제**: `f1f41d5`가 확장 ROI 도입과 함께 의자 연결 호출 4개를 제거했고, `70a86bc`("ROI 크기 원복")가 ROI만 되돌리고 의자 연결은 복구하지 않음. 결과적으로 `seat_detections`가 항상 빈 리스트(`engine/seatnow_core.py:1282`, `:1301`).

**죽어 있는 기능**
1. 판정 규칙 3번 — 의자→테이블 점유 전파 (`engine/seatnow_core.py:850`에서 `_ = occupied_chairs`로 인자 폐기)
2. `inferred-seat` — 가려진 테이블 구제 (`:1518`의 `has_seat_support()`가 항상 False)
3. 포즈 의자겹침 구제 (`:1331`의 `seat_support_score()`가 항상 0.0)
4. 레이아웃 모드의 캘리브레이션된 의자 존 (`LayoutZoneTracker`에 빈 리스트 전달, `:1259`)
5. 죽은 함수 4개 — `associate_chairs_to_tables`, `filter_strong_chair_links`, `associate_objects_to_chairs`, `associate_seated_people_to_chairs`. **테스트에서만 호출되어 회귀가 안 잡혔음**

**특히 치명적인 연쇄** — 카페 CCTV에서 앉은 손님은 무릎·발목이 테이블에 가린다:
```
각도 측정 불가 → compact_occluded_pose 폴백(SEATED)  engine/seatnow_core.py:468
  → analyze()에서 무조건 UNKNOWN 강등                :1338
  → 구제 경로(seat_support ≥ 0.24)가 회귀로 사망      :1339
  → 영원히 UNKNOWN → 테이블도 OCCUPIED가 아닌 UNKNOWN :853
```
**이건 파인튜닝으로 못 고친다.** 모델이 완벽해도 같은 경로를 지난다.

**선택지**
- **(a) 의자 연결 복구** ← 채택 권고. 카메라 1대 = 가림 많음이라 구제 경로가 중요
- (b) 확장 ROI 재도입 — 마진을 x0.45/y0.60의 절반쯤으로 줄여 재튜닝
- (c) 정식 제거 — 죽은 코드·`TableObservation` 필드·JSONL 스키마·문서 정리. 단 위 UNKNOWN 강등을 다른 방식으로 풀어야 함

**손댈 파일**: `engine/seatnow_core.py:1282`, `:1301`, `:850`, `:1331`, `:1518`, `:1259`
**예상**: 1~2일
**완료 기준**: `sample_raw/`의 "의자 위 가방"·"짐 옮김" 시나리오에서 점유가 다시 잡힘 + 회귀 테스트 추가

---

### T2. 진단 로깅 추가

**문제**: `frame_log_record()`(`engine/seatnow_core.py:3143`)가 추적된 트랙만 남기고 **원본 detect 출력(`FrameAnalysis.detections`)을 기록하지 않음.** 그래서 "모델이 못 봤나(파인튜닝 영역)" vs "코드가 버렸나(`select_table_candidates`)"를 로그로 구분할 수 없음.

**할 일**: JSONL에 raw detection 요약 필드 추가 (클래스·conf·박스). 용량 우려 시 `--debug` 또는 별도 플래그 게이트.

**손댈 파일**: `engine/seatnow_core.py:3143`, `engine/seatnow.py`
**예상**: 0.5일
**완료 기준**: 미탐 테이블 1건에 대해 로그만으로 A/B 판별 가능

---

### T3. 평가셋 확장

> ⚠️ **2026-08-27 정정**: 이 절이 전제한 "1시간 영상 + 엣지케이스 10분"은 실제로
> 존재하지 않는다. 보유 영상의 실측 현황과 그에 따른 계획 변경은 **§0-b**와
> **§4 T16**을 볼 것. 아래 "할 일"의 코드 부분은 완료됐고, 데이터 부분만 남았다.

**문제**: fixture가 20초 1개이고 영상 sha256에 고정(`checks/verify_seatnow.py:43-140`). 보유 영상은 라벨이 없어 채점 불가. 기록된 정확도 5/20(25%)로는 파라미터 A/B 차이를 구분할 수 없음. 기존 fixture는 고정 CCTV가 아니라 **좌→우 팬 촬영 스톡 영상**이라 대표성도 없다.

**할 일**
- 다중 영상 fixture를 받도록 `checks/verify_seatnow.py` 구조 변경
- 보유 영상 구간 라벨링 (시작/종료 초 + 좌석별 점유 상태)
- 커버리지 집계 추가: `raw_state=IGNORE` 비율, `missing_count` 분포 → 화각이 못 보는 좌석 정량화

**손댈 파일**: `checks/verify_seatnow.py:43-140`, `tests/fixtures/`
**예상**: 3~4일
**완료 기준**: 영상 2개 이상에 대해 프레임 정확도 산출, T1 전후 비교 가능

---

## Phase 2 — 엣지 준비 (박스 도착 전 완료)

### T4. OpenVINO 익스포트 + 벤치 스크립트

**문제**: ultralytics `.pt`를 직접 로드(`engine/seatnow_core.py:1079-1080`). 익스포트 경로 0건. yolov8n 실측 latency 기록 0건 (유일한 기록은 yolov8x "프레임당 ~8초 on CPU", `README.md:54`).

**할 일**
- `edge/export.py` — `.pt` → OpenVINO FP32 → INT8
- `edge/bench.py` — `{detect, pose} × {640, 960, 1280} × {pt, ov-fp32, ov-int8}` latency 표 → tick 예산 자동 산출
- 모델 로딩부를 백엔드 교체 가능하게 분리
- 맥북 baseline 먼저 측정 (상대 비교용)

**tick 예산 계산 근거** (기본값 기준):
```
median_frames=2 → 5프레임
프레임당: detect@1280 + pose@960 + 크롭 최대 4회@960 = 최대 6회
→ tick당 최대 30회 추론 / 15초 예산 → 추론 1회당 0.5초 이내여야 함
```

**손댈 파일**: 신규 `edge/export.py`, `edge/bench.py`, `engine/seatnow_core.py:1079-1080`
**예상**: 2~3일
**완료 기준**: 맥북에서 표가 출력됨. 엣지 도착 시 같은 명령으로 재실행 가능

---

### T5. RTSP 재송출 테스트 하네스

**목적**: **카메라 없이** 전체 파이프라인(디코딩·추론·RTSP 리더)을 검증하기 위함. 샘플 영상을 로컬 RTSP로 쏘고 엣지가 그걸 수신.

**할 일**: `mediamtx` 또는 `ffmpeg -f rtsp` 기반 재송출 스크립트 + 사용법 문서화

**예상**: 0.5일
**완료 기준**: 맥북에서 쏜 RTSP를 다른 기기에서 VLC로 수신 확인

---

## Phase 3 — 엣지 실측 (박스 도착 후)

### T6. Phase 1 벤치 실행

1. **QSV 디코딩 단독 검증** — `ffmpeg -hwaccel qsv`로 RTSP 1080p 연속 디코딩 시 CPU 사용률. **30% 넘으면 QSV가 안 켜진 것**
2. **추론 latency 표** — T4의 `edge/bench.py` 실행
3. **tick 예산표 완성** → "이 박스의 최대 프로파일" 확정

**합격 기준**
| 등급 | 기준 |
|---|---|
| 하드 실패 | 최소 프로파일(추론 2회 @640)조차 15초 초과 → 하드웨어 교체 |
| 조건부 | 목표 프로파일이 tick의 50~100% (7.5~15초) |
| **합격** | 목표 프로파일이 **tick의 50% 이내 (7.5초)** |

> 여유 50%를 요구하는 이유: 24/7 상시 가동이라 RTSP 재연결·디코더 리셋·로그 회전이 언제든 끼어든다.

**예상**: 2~3시간

---

### T7. 파라미터 스윕

**할 일**: `(median_frames × sample_seconds × imgsz × table_crops)` 그리드를 평가셋에 돌려 **정확도 × 추론시간** 표 생성. 곡선에서 "15초 안에 들어오면서 정확도 최대"인 지점 선택.

**전제**: T1(기준선) + T3(평가셋) + T6(예산) 완료

**손댈 파일**: 신규 `edge/bench_sweep.py`, `checks/verify_seatnow.py`
**예상**: 3~4일
**완료 기준**: 배포 프로파일 1개가 근거와 함께 확정됨

---

## Phase 4 — 라이브 파이프라인

### T8. RTSP 스트림 리더 ⚠️ 아키텍처 변경

**문제 (단순 URL 교체가 아님)**
- 입력이 로컬 파일로 하드 검증됨 (`engine/seatnow.py:118`의 `args.input.exists()`, `:580-581`의 확장자 검사)
- **`FFmpegBurstReader`가 tick마다 ffmpeg를 새로 띄워 `-ss`로 시크**(`engine/seatnow_core.py:2760-2782`, `-ss`가 `-i`보다 앞 = 입력 시크)
- **라이브 스트림은 시크 불가.** `-ss 900`을 라이브에 걸면 900초를 기다린다

**할 일**
- 링버퍼형 `StreamBurstReader` 신설 — 연속 디코딩하며 최근 N프레임 보유, tick 시 꺼냄
- 입력 검증 완화 (RTSP URL 허용)
- 버스트 간격 재검토: 현재 ±N 네이티브 프레임(30fps에서 167ms 스팬). 파이프 대역폭 절감 + 탈상관 관점에서 `-vf fps=5` 기반 200ms 간격이 나을 수 있음 → T7에서 검증

**손댈 파일**: `engine/seatnow_core.py:2732-2825`, `engine/seatnow.py:118`, `:580`
**예상**: 3~5일
**완료 기준**: T5의 로컬 RTSP를 입력으로 전체 파이프라인이 tick 단위로 동작

---

### T9. Quick Sync 하드웨어 디코딩

**문제**: ffmpeg 커맨드(`engine/seatnow_core.py:2664-2683`, `:2760-2782`, `:2829-2860`)에 `-hwaccel` 계열 플래그가 **0건.** 현재 100% 소프트웨어 디코딩. **씬클라이언트를 고른 이유(Quick Sync)가 코드에 반영되어 있지 않음.**

**할 일**: `-hwaccel qsv` (Linux는 VA-API 드라이버 `intel-media-va-driver` 포함) 추가. tick마다 프로세스를 새로 띄우면 디코더 초기화 비용이 반복되므로 T8의 상시 디코딩 구조와 함께 적용.

**손댈 파일**: `engine/seatnow_core.py:2664`, `:2760`
**예상**: 1일
**완료 기준**: T6-1의 CPU 사용률이 유의미하게 하락

---

### T10. 배포 프로파일

**할 일**
- **`--no-video`를 배포 기본값으로** ⚠️ 현재는 주석 MP4를 항상 디스크에 씀(`engine/seatnow.py:218-222`). 운영에서 그대로 두면 **"영상 미저장" PIPA 예외 조항이 깨진다**
- systemd 유닛 + 자동 재시작 (현재 배포 파일 전부 없음)
- 로그 회전
- 원격 접속(Tailscale 등) — 필요 시

**예상**: 1~2일

---

## Phase 5 — 조건부

### T11. 파인튜닝 (조건 충족 시에만) ⚠️ 확정 사항 아님

**선행 조건 3개 — 하나라도 빠지면 시작하지 말 것**
1. T1 완료 (정확도 기준선)
2. T3 완료 (측정할 자)
3. T6 완료 (**목표 imgsz 확정** — 640용 모델과 1280용 모델은 학습 설정이 다르다)

**판단 지점**: T7의 배포 프로파일에서 정확도가 충분하면 **파인튜닝은 불필요하다.** T1 복구만으로 크게 올라올 가능성이 있고, 라벨링은 이 플랜에서 가장 비싼 노동(300~600장)이다.

**실행 시 유의**
| 항목 | 내용 |
|---|---|
| detect 모델 | 파인튜닝 대상 ✅ |
| pose 모델 | **사실상 불가** ❌ — keypoint 17개 라벨링은 비용 차원이 다름. COCO 유지 |
| 데이터 | 1시간 영상 → 2초당 1프레임 = 1,800장. 현실적 라벨링 300~600장. Roboflow 공개셋을 베이스로 |
| 클래스 축소 | COCO 80 → 12개 내외(person, chair, dining table, couch, bench, cup, bottle, laptop, cell phone, handbag, backpack, book). 헤드가 작아져 속도 이득 |
| ⚠️ 주의 | 클래스 축소 시 `EXCLUDED_OBJECT_CLASSES`(`engine/seatnow_core.py:43-79`)와 클래스명 매핑을 함께 수정. **이름이 바뀌면 판정 로직이 조용히 오작동한다** |
| 환경 | Google Colab (T4) |

**파인튜닝의 두 가지 용도 — 지금은 전자**
```
                    ┌─→ 정확도에 쓴다   같은 속도로 더 정확하게   ← 현재 단계
파인튜닝 = 여유 확보 ─┤
                    └─→ 속도에 쓴다     같은 정확도로 더 싸게     ← 확장 단계(매장 수십~100곳)
```

**예상**: 2~3주

---

## Phase 6 — 출력 계약 (2026-08-27 완료)

### T13. `seat_report` 출력 계약 + 바형 좌석 ✅ 완료

**스펙**: `docs/superpowers/specs/2026-08-27-seat-report-contract-design.md`
**계획**: `docs/superpowers/plans/2026-08-27-seat-report-contract.md`
**커밋**: `176f696` … `56b8b88` (9개 작업, 테스트 181 → 229)

**푼 문제 3가지**

1. **일자형/벽 책상이 출력에 존재하지 않았다.** 모델이 `dining table`로 못 잡고,
   사람이 그려 넣어도 판정 단위가 "테이블 1개 = 상태 1개"라 "6석 중 3석"을 표현
   할 수 없었다.
2. **앱이 쓸 계약이 없었다.** JSONL 최상위가 트랙 단위 진단 정보(`tables[]`)라
   "몇 자리 남음"으로 변환되지 않았다.
3. **UNKNOWN을 줄이려 해도 어디를 잡을지 몰랐다.** 원인이 되는 포즈 사유가 테이블로
   올라오며 `nearby_person_pose_unknown` 한 줄로 뭉개졌다.

**들어간 것**

| 항목 | 내용 |
|---|---|
| 레이아웃 스키마 v2 | `kind: "counted_zone"` + `seats[]`. `capacity`는 `len(seats)`에서 파생 |
| 판정 단위 평탄화 | `judgement_units()` — 바 구역이 좌석 칸마다 독립 관측이 됨 |
| 걸침 규칙 | 한 사람이 칸 2개 이상에 걸치면 그 칸 전부 UNKNOWN(`spans_multiple_seats`) |
| 사유 코드 | `engine/seatnow_report.py`의 닫힌 Enum. `install`/`geometry`/`model`/`time`/`settled` |
| 앱 계약 | `seat_report` — tick마다 JSONL 최상위. `tables[]`는 진단용으로 유지 |
| 캘리브레이션 | `install/calibrate.py`에 `[z]`(바 구역) `[x]`(자리 칸) 모드 |
| 채점 | `verify_seatnow.summarize_unknown_reasons()` — UNKNOWN을 그룹별로 분해 |

**핵심 구현 판단**: 좌석 칸을 **판정 단위로 평탄화**해서 기존 증거 연결·디바운싱이
칸마다 그대로 돌게 했다. 새 카운트 로직을 만들지 않았고, 디바운싱(점유 2회/이탈
3회)도 칸마다 공짜로 따라왔다.

**실제 출력 예시**
```
앱 표시: 총 6석 중 2자리 남음 (확인 안 됨 2석)
```
```jsonc
"seat_report": {
  "seats": [
    {"seat_id": "창가1", "kind": "table", "state": "occupied",
     "reason_code": "belongings"},
    {"seat_id": "바", "kind": "counted_zone", "capacity": 4,
     "occupied": 1, "free": 1, "unknown": 2,
     "reason_codes": {"spans_multiple_seats": 2}}
  ],
  "totals": {"capacity": 6, "occupied": 2, "free": 2, "unknown": 2}
}
```

---

## 상시

### T12. 문서 갱신
- ~~`README.md` "유닛 테스트 54개" → 117개~~ → **2026-08-27 기준 229개로 갱신 완료**
- ~~`README.md` "레이아웃 구현 중"~~ → 이미 머지됨
- `SEATNOW_전체정리.md` 배포 구조에 **"POS기 제외" 근거** 추가 (아래 결정 기록 참조)
- **남은 것**: `SEATNOW_전체정리.md`에 T13 출력 계약(`seat_report`)과 바형 좌석
  반영. 앱 연동 담당자가 읽을 계약 문서가 아직 스펙 파일에만 있다

**예상**: 0.5일

---

## 2. 하지 않기로 한 것 (결정 기록)

같은 논의를 반복하지 않기 위해 근거와 함께 남긴다.

### POS기에 설치 — ❌ 파일럿 단계에서 제외
`SEATNOW_전체정리.md`의 MVP 계획 "카페 기존 PC에 소프트웨어 설치"에서 **"기존 PC" ≠ "POS기"**로 구분한다.
1. **POS는 매장 매출의 단일 장애점.** 결제가 밀리는 사고가 한 번만 나도 그 매장을 잃는다
2. POS PC 사양이 대체로 우리 워크로드보다 낮음 (Celeron/Atom, RAM 4GB, Win10 IoT)
3. POS 벤더가 임의 설치를 금지·보증무효화하며, 원격 업데이트로 우리 프로세스를 죽일 수 있음. 결제 보안 규정 위반 소지
4. 결제 단말 + 영상 처리 결합 = 사고 시 파장 증폭. "사장님이 운영 주체" 3원칙과도 충돌(POS는 벤더 관리)
5. 랜포트 없음, OS 파편화, 파일럿 섭외 난이도 상승

**나중에 옵션 B로 검토할 조건**: Intel 6세대 이상 i3급 + 여유 RAM 4GB + **벤더 서면 허용** + 프로세스 우선순위/CPU 상한 격리 + 원인 판별 로깅.
**더 나은 대안**: 매장의 POS 아닌 다른 PC(사무용·사이니지용)가 있으면 그쪽이 훨씬 안전하다.

### 카메라 2대 — ❌ 1대 확정
커버리지 부족이 확인되면 재검토. T3의 IGNORE 비율·`missing_count` 집계가 판단 근거.
`CLAUDE.md`에 규칙으로 고정했다.

### 카메라 설치를 매장에 맡기기 — ❌ 우리가 고르고 우리가 단다
기종·위치·각도·높이 전부 SeatNow 팀이 정하고 직접 설치한다. 그래서 **화각이 못 보는
좌석(IGNORE)은 제품의 한계가 아니라 설치 결함**이고, 코드로 보정하지 않고 카메라를
다시 단다. 반대로 UNKNOWN(보이는데 판정 실패)은 코드로 푼다. **이 둘을 섞지 않는
것**이 T13 사유 코드 체계의 출발점이다. (`CLAUDE.md`)

### 매장별·각도별 임계값 튜닝 — ❌ 설계에서 금지
매장에 요구할 수 있는 수작업은 **설치 당일 `install/calibrate.py` 검수 1회가 전부**다
(`_preseed`가 자동으로 채운 것에서 잘못 잡힌 것 지우고, 못 잡은 것 추가). `AnalysisSettings`
기본값 하나로 굴러가야 한다. 운영 중 재캘리브레이션을 전제한 설계도 만들지 않는다.

### 바 구역을 "증거 덩어리 개수"로 세기 — ❌ 사람이 칸을 긋는다
처음에는 바 전체를 한 구역으로 두고 사람/짐 덩어리 수를 세는 안을 잡았으나 폐기했다.
**두 명이 붙어 앉으면 한 덩어리로 보여 1명으로 세고, 그러면 빈자리를 부풀려 "자리
있음"을 잘못 내보낸다.** 3상태 정직 노출 원칙과 정면으로 어긋난다. 대신 설치 때
사람이 자리마다 칸을 긋고, 한 사람이 칸 2개에 걸치면 UNKNOWN으로 낸다.
자동 등분은 원근 때문에 안 된다 — 사람이 화면을 보고 그으면 원근이 이미 반영된다.

### 판정 임계값의 각도 불변화 (`sitting_angle` 등) — ❌ 지금은 안 한다
근본적이지만 근거가 없다. 어느 임계값이 실제 병목인지는 라벨링된 평가셋에서 사유
코드 분포(`geometry` vs `model`)가 나온 뒤에 판단한다. 설치 후 각도는 어차피 고정이고,
"어떤 각도에서도 되는 알고리즘"보다 **"좋은 각도를 고르는 설치 기준"**이 먼저다.

### 지금 파인튜닝 시작 — ❌ 조건부로 연기
T1/T3/T6 미완료 상태에서 라벨링을 시작하면, **회귀로 생긴 실패를 "모델 탓"으로 오해해 엉뚱한 데이터에 노동을 쏟게 된다.**

### 카메라 즉시 구매 — ❌ 엣지 벤치 후
T6에서 나온 최대 프로파일이 있어야 해상도(2MP/4MP)를 제대로 고른다. 4MP를 샀는데 엣지가 못 버티면 돈이 새고, 2MP인데 여유가 남으면 화질을 손해 본다.
구매 시 **RTSP 개방 여부 확인 필수** — 하이크비전/다후아 계열은 확정, 국내 저가 브랜드는 검증 필요.

> **2026-08-31 정정 — "추론 벤치로 해상도를 고른다"는 틀렸다.**
> `edge/bench.py`가 재는 추론은 프레임을 `imgsz`(640/960/1280)로 **줄여서** 모델에 넣는다.
> 그래서 **카메라가 2MP든 8MP든 추론 시간이 거의 같다.** 이 표만으로는 해상도를
> 고를 근거가 나오지 않는다.
>
> 해상도가 실제로 잡아먹는 것은 **디코딩** — 압축된 영상을 픽셀로 푸는 일이고,
> tick마다가 아니라 **24시간 내내** 돈다. 그런데 저장소에는 그것을 재는 코드도,
> 하드웨어 디코딩 옵션(`-hwaccel`)도 없었다. 있는 그대로 쟀으면 소프트웨어 숫자만
> 나와서 4MP를 부당하게 탈락시켰을 것이다.
>
> → **T9**(하드웨어 디코딩)와 **T17**( `edge/bench_decode.py`)로 해결. 카메라 선정 근거는
> 이제 `edge/bench_decode.py`가 내는 **합산 표**(디코딩 + 추론)다.
> 절차는 `docs/edge-setup.md`, 설계는
> `docs/superpowers/specs/2026-08-31-edge-decode-bench-design.md`.

---

## 3. 요약 일정

| Phase | 작업 | 예상 | 하드웨어 필요 |
|---|---|---:|:---:|
| 1 | T1 회귀 복구 | 1~2일 | — |
| 1 | T2 진단 로깅 | 0.5일 | — |
| 1 | T3 평가셋 | 3~4일 | — |
| 2 | T4 익스포트+벤치 | 2~3일 | — |
| 2 | T5 RTSP 재송출 | 0.5일 | — |
| 3 | T6 엣지 벤치 | 2~3시간 | 엣지 |
| 3 | T7 파라미터 스윕 | 3~4일 | 엣지 |
| 4 | T8 RTSP 리더 | 3~5일 | 엣지 |
| 4 | T9 Quick Sync | 1일 | 엣지 |
| 4 | T10 배포 프로파일 | 1~2일 | 엣지 |
| 5 | T11 파인튜닝 | 2~3주 | 조건부 |
| — | T12 문서 | 0.5일 | — |
| 6 | T13 출력 계약 ✅ | 완료 | — |
| 4 | T9 Quick Sync ✅ | 완료 | — |
| — | T17 디코딩 벤치 ✅ | 완료 | — |
| — | **T14 실영상 첫 실행** | **1~2시간** | — |
| — | **T15 설치 각도 선정** | **반나절** | — |
| — | **T16 라벨링** | **2~3시간** | — |

**현재 병목은 코드가 아니라 관측이다.** T14~T16은 하드웨어 없이 오늘 당장 할 수 있고,
그게 끝나야 "모델이 이 카페를 얼마나 보는가"라는 가장 큰 미지수가 풀린다.

> **2026-08-31**: T14의 코드·설치 부분은 끝났다. 남은 것은 캘리브레이션 창에서
> 좌석을 그리는 **사람 작업**이다. 무엇을 언제 하는지는 **§4가 유일한 출처**다.

---

## 4. 다음 액션 (2026-08-31 기준)

> **이 절이 "지금 무엇을 할 차례인가"의 유일한 출처다.** 다른 절과 어긋나면 이 절이 맞다.

남은 일은 **두 갈래고, 서로 기다리지 않는다.** 한쪽이 막혀도 다른 쪽은 진행된다.

```
트랙 A (지금 당장, 하드웨어 불필요)
  T14 좌석 레이아웃 그리기 ──> T15 설치 각도 선정 ──> T16 라벨링 ──┐
                                                                    ├──> T7 스윕
트랙 B (엣지 박스 도착 후)                                          │      ↓
  edge/check_edge.py ──> edge/bench.py ──> edge/bench_decode.py ──> 카메라 확정 ───┘   T11 판단
                                                          ↓
                                                  T8 RTSP 리더 ──> T10 배포
```

**트랙 A의 순서는 지켜야 한다.** 라벨링을 먼저 하면 설치 각도가 안 정해진 상태에서
라벨을 만들게 되고, 각도가 바뀌면 그 노동이 통째로 버려진다. 이 플랜에서 가장 비싼
사람 노동이다.

**트랙 B는 박스가 오기 전까지 할 게 없다.** 도구는 다 만들어져 있다.

### 지금 당신이 할 일 (2026-09-01(3) 기준)

**1. angle1 사진 12장을 한 장씩 확인한다** (지금 하는 일)

`results/angle1_layout/review/` 를 탐색기로 열고 방향키로 넘긴다. `★` 5장이
우리와 Codex가 다른 장면이다. 보는 법은 그 폴더의 `00_읽는법.md`.

특히 봐야 할 것:

- 파란 상자가 **없는 사람**이 있는가 → 우리가 놓친 사람
- 사람이 없는데 파란 상자가 있는가 → 헛것
- **초록(빈자리)이 정말 비어 있는가** → 이게 틀리면 손님을 남의 자리로 보낸다
- **짐이 놓인 테이블** → Codex가 못 가린 부분이라 사람 눈이 유일한 정답지다

**2. 그 다음: 평면도를 실제로 그린다** (보류 중)

편집기는 다 만들어져 있다.

```bash
./venv/Scripts/python.exe -m install.floorplan_editor --layout layouts/cafe_angle1.json \
  --log results/angle1_layout/log.jsonl
```

`[벽 그리기]`로 방 전체를 대각선으로 끌고, `[입구]` `[카운터]` `[소파]` `[창문]`을
같은 방식으로 그린다. 테이블·의자는 한 번 누르면 크기·회전 손잡이가 나온다.
`[2인 의자]`, `[의자 지우기]`(지도에서만 뺀다)도 있다. 다 맞추면 `[저장]`.

**주의: 바닥 네 점이 잘못 찍혀 있어서(§0-e) 처음 위치는 많이 틀리다.**
특히 T4·T5·바가 엉뚱한 곳에 있다. 손으로 옮기면 된다.

---

### T14. 실영상 첫 실행 — ✅ 완료 (2026-09-01)

| 단계 | 상태 |
|---|---|
| 모델 의존성 설치 (`torch 2.2.2` + `ultralytics 8.4.82`) | ✅ 2026-08-31 |
| 테스트 회귀 확인 (291개 통과) | ✅ 2026-08-31 |
| `_preseed`가 각도별로 무엇을 잡는지 조사 | ✅ 2026-08-31 → §0-c |
| 좌석 레이아웃 그리기 → `layouts/cafe_angle1.json` | ✅ 2026-09-01 (테이블 6, 창가 6석, 의자 25) |
| 검출 검사 하네스로 angle1~4 완주 + 레이아웃 전후 비교 | ✅ 2026-09-01 → §0-c |

**끝났다.** 아래는 다음 매장에서 반복할 절차다. `install/calibrate.py`는 마우스로 그리는 창이라
(`install/calibrate.py:306-307`) 자동화 대상이 아니고, `CLAUDE.md`가 허용한 "설치 시 1회
검수" 그 자체다.

```bash
./venv/Scripts/python.exe -m install.calibrate sample_raw/cafe_sample_angle1.mov --output layouts/cafe_angle1.json
#   키: [t]able [c]hair [z]one seat[x] [g]en-seats [f]loor [m]ove [d]elete [u]ndo [s]ave [q]uit
```

**창에서 할 일 3가지** — §0-c에서 이미 드러난 것을 근거로 좁혔다.

1. **의자 과탐을 지운다.** 같은 의자에 상자가 여러 개 겹쳐 잡힌다. 가장 많이
   손댈 부분이다
2. **일자형/벽 책상을 직접 그린다.** angle2 왼쪽 벽면의 긴 벤치+목재 책상이
   그 대상이다(§0-c). `[z]`로 바 구역을 치고 `[x]`로 **자리마다 칸을 긋는다.**
   칸 개수 = 자리 수다. 자동 등분은 원근 때문에 안 되므로 화면을 보고 그어야 한다
3. **엉뚱하게 잡힌 것을 지운다** — 선반·카운터 등

**완료 기준**: 저장 시 출력되는 `테이블 N개, 의자 M개, 바 구역 K개(S석)`가
눈으로 센 것과 일치한다.

**예상**: 30분~1시간 (설치는 이미 끝났다)

---

### T15. 설치 각도 선정

**목적**: angle1~4 중 **실제로 카메라를 달 각도 하나**를 고른다. `CLAUDE.md`대로
카메라는 우리가 고르고 우리가 단다.

```bash
# 각도 4개에 각각 캘리브레이션 (T14를 나머지 3개에 반복)
for a in 1 2 3 4; do
  ./venv/Scripts/python.exe -m install.calibrate sample_raw/cafe_sample_angle${a}.mov \
    --output layouts/cafe_angle${a}.json
done

# 각도별로 실제 분석 실행 (78초 영상이라 tick을 줄여 표본을 늘린다)
for a in 1 2 3 4; do
  ./venv/Scripts/python.exe -m engine.seatnow sample_raw/cafe_sample_angle${a}.mov \
    --layout layouts/cafe_angle${a}.json --sample-seconds 5 \
    --log-detections --no-video
done

# 사유 코드 분포 비교
./venv/Scripts/python.exe -m checks.verify_seatnow results/*.jsonl
```

**판단 기준 — 이 순서로 본다**

| 순위 | 지표 | 뜻 |
|---|---|---|
| 1 | **커버되는 좌석 수** | 그 각도로 실제 좌석 몇 개가 화각에 들어오는가 |
| 2 | **IGNORE 비율** | 화각이 못 보는 좌석. 낮을수록 좋다 (설치 품질 지표) |
| 3 | **`geometry` 그룹 UNKNOWN** | 가림이 얼마나 심한가 |
| 4 | `model` 그룹 UNKNOWN | 모델이 못 보는 정도 — T11 판단 근거 |

> `checks/verify_seatnow.py`는 라벨(fixture) 없이도 커버리지·사유 분포를 낸다. 정확도만
> 라벨이 필요하다. **각도 선정에는 라벨이 필요 없다.**

**완료 기준**: 각도 하나가 근거(위 4개 지표 표)와 함께 확정된다.
**동시에 나오는 부산물**: 그 각도의 IGNORE 비율이 카메라 1대 결정(§2)의 재검토 근거.

**예상**: 반나절

---

### T16. 라벨링 (T3의 데이터 부분)

**⚠️ T15 완료 전에 시작하지 말 것.** 각도가 바뀌면 라벨이 통째로 버려진다.

**먼저 결정할 것**: `moonq_sample_longform.mp4`(10.5분, 720p/120fps)가 **확정된 설치
각도와 같은 위치의 고정 촬영인가?**

- **그렇다** → 이게 평가셋이다. 10초 간격 = 63 interval. 바로 라벨링
- **아니다** → 확정 각도로 **30분~1시간 재촬영이 필요하다.** angle 영상 4.8분(18틱)
  으로는 T7 파라미터 스윕의 A/B 차이를 가릴 수 없다

```bash
./venv/Scripts/python.exe -m checks.make_labels sample_raw/<확정영상> --interval 10 \
  --contact-sheet labels/<이름> --layout layouts/<확정레이아웃>.json
# labels/<이름>/*.jpg 를 보며 각 interval의 occupied / empty / ignore 를 채운다
./venv/Scripts/python.exe -m checks.make_labels x \
  --validate tests/fixtures/<이름>_expectations.json
```

- 모든 좌석은 매 interval마다 세 목록 중 **정확히 하나**에 들어가야 한다.
  누락을 empty로 오해하지 않기 위한 규칙이며 `--validate`가 잡아준다
- **바 구역은 자리 칸마다 한 줄씩 나온다** (`BAR-1`, `BAR-2`, …). T13에서 라벨
  대상이 `judgement_units()` 기준으로 바뀌었다
- `ignore`는 회피가 아니라 **화각이 판단 불가한 좌석**의 라벨이다
- **상태 전이(`expected_events`)는 비워둔다.** 이탈 확정에 45초가 필요해 짧은
  영상으로는 채점이 성립하지 않는다 (§0-b)

**예상**: 63 interval 기준 2~3시간

---

### 트랙 B — 엣지 박스가 도착하면 (하드웨어 필요)

```
엣지 도착 ──> edge/check_edge.py ──> edge/bench.py ──> edge/bench_decode.py ──> 카메라 확정
                  │                                                   │
            (드라이버 못 잡으면                                   T8 RTSP 리더
             여기서 멈추고 해결)                                       │
                                                                 T10 배포 프로파일
```

절차 전문은 `docs/edge-setup.md`. 요약하면 세 줄이다.

```bash
python -m edge.check_edge       # 이 박스가 돌릴 수 있는가 (항목별 합격/불합격)
python -m edge.bench --frames sample_raw/cafe_sample_angle1.mov --label edge-box
python -m edge.bench_decode --source sample_raw/cafe_sample_angle1.mov
```

** `edge/check_edge.py`에서 하드웨어 디코딩이 "꺼짐"으로 나오면 거기서 멈추고 드라이버부터
잡는다.** 꺼진 채로 재면 비용이 3~6배로 나와서 살 수 있는 카메라를 못 산다고 잘못
판단한다. Linux는 `intel-media-va-driver-non-free`, Windows는 인텔 그래픽 드라이버다.

**OpenVINO 익스포트를 먼저 하는 편이 낫다** (`python -m edge.export`). 실제 배포는
OpenVINO로 하므로 `pt` 숫자는 참고치일 뿐이다.

#### 파인튜닝 판단은 엣지로 안 나온다

엣지가 답하는 건 "얼마나 빠른가"다. 파인튜닝은 "모델이 이 카페를 얼마나 못 보는가"
의 문제이고, 그 답은 **T14~T15**(노트북, 하드웨어 불필요)의 UNKNOWN 사유 코드 분포에서
나온다. `model` 그룹이 크면 파인튜닝, `geometry` 그룹이 크면 카메라 각도를 다시 고른다.
**두 갈래는 병렬로 진행할 수 있다.**

---

---

### 두 트랙이 만나는 지점

**트랙 A(T16 라벨링)와 트랙 B(카메라 확정)가 둘 다 끝나야** `edge/bench_sweep.py`로 T7을
돌려 배포 프로파일을 확정할 수 있다. 정확도를 재려면 라벨이 필요하고, 속도 예산을
알려면 박스 숫자가 필요하기 때문이다.

그 다음 순서는 이렇다.

1. **T11 파인튜닝 여부 판단** — T7에서 나온 UNKNOWN 사유 코드 분포를 본다.
   `model` 그룹이 크면 파인튜닝, `geometry` 그룹이 크면 카메라 각도를 다시 고른다
2. **T8 RTSP 리더** — 카메라를 꽂아 돌리려면 반드시 필요하다.
   지금 코드는 파일 전용이고, tick마다 영상을 특정 시각으로 되감는 구조라
   **라이브 스트림에서는 동작하지 않는다**
3. **T10 배포 프로파일** — 자동 재시작·로그 회전·`--no-video` 기본값

~~T9(Quick Sync)~~는 2026-08-31 완료.

---

## 5. 열린 질문

다음 세션에서 먼저 답해야 할 것들.

1. **`moonq_sample_longform.mp4`는 무엇인가?** 720p/120fps로 다른 넷과 촬영 장비가
   다르다. 같은 카페인가? 고정 촬영인가? → T16의 진로가 갈린다
2. ~~**파일럿 카페에 일자형/벽 책상이 실제로 몇 개, 몇 자리인가?**~~ → **부분 해소.**
   angle2 왼쪽 벽면의 긴 벤치 + 긴 목재 책상으로 특정됐다(§0-c). 자리 수는 설치
   검수 때 사람이 세어 칸을 긋는다
3. **엣지 박스는 도착했나?** T6이 여기 걸려 있다. 도착하면 §6의 세 줄
4. **카메라 설치 기준(높이·부각·좌석까지 거리)** — T15에서 각도를 고르고 나면
   그 기준을 `CLAUDE.md`에 적어 다음 매장에 재사용한다
